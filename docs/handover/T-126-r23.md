# T-126 handover — M3 gate r23: 5/5 then 1/5 then 0/5 same binary, variance is the finding

**For:** next session continuing M3 gate or Phase-4.
**Written:** 2026-09-14, after four legs (300s+900s+300s+900s) on new binary.
**Supersedes:** T-125-r18-r22.md §5.1-5.2 (r21 re-run + extra node).

## 0. Read first

- **Reach**: first two legs 5/5 at ~150s (was r21 4/5 at 232s) — improvement. Next two legs same binary: 1/5 and 0/5 — **leg variance large**, same as r22 0/5 vs r22b 2/5.
- **Boss verdict still NOT established** — no kills, ever.
- **New config**: map-5 route 5 nodes `6,21 → 13,10 → 19,10 → 22,10 → 22,3` + kiter band on map5 with front-runner guard.
- **Epoch 21, wire 237, schema v11, suite 209/209, duel pin b273be661b54673a** — bots-only.

## 1. The four legs (all replay mm=0)

| leg | secs | map5 entries | reach (s) | elites / trash | deaths | replay | file |
|-----|------|--------------|-----------|----------------|--------|--------|------|
| r23 smoke #1 | 300 | 5/5 | 147.3 / 149.9 / 151.9 / 149.0 / 152.3 | 3,4,4,4,3 / 8-9 | 6/4/4/2/6 | ticks=6042 cmds=1957 hashes=60 mm=0 ent=173 | overwritten, log in bash output 09:00Z |
| r23 gate #1 | 900 | 5/5 | 145.8 / 150.3 / 150.0 / 152.3 / 153.8 | 1,5,5,5,2 / 5-8 | 8/6/10/6/10 | ticks=18042 cmds=4979 hashes=180 mm=0 ent=189 | overwritten, log in bash output 09:15Z |
| r23 smoke #2 | 300 | 1/5 | 188.1 | 0 / 0-2 | 6/4/6/2/6 | ticks=6043 cmds=1842 hashes=60 mm=0 ent=175 | `logs/m3_gate_r23.bwj` (36K) |
| r23 gate #2 | 900 | 0/5 | — | 0 / 0 | 8/10/10/8/10 | ticks=18042 cmds=4611 hashes=180 mm=0 ent=187 | `logs/m3_gate_r23b.bwj` (92K) |

Staging: L13 / 7-11-6 / Pit Blade 2002 + Hide Armor 2101 / 16 vials / map3 (1,22), class_id seeded (00/04→1, 01/03→3, 02→2). Server down for DB writes, topup idempotent.

Journals: `logs/m3_gate.bwj` is r17 (124K, 36042 ticks, 5933 cmds, 360 hashes, mm=0) — restored from master. r23 files are variance evidence.

## 2. What moved

- **Node (19,10)**: walkable (gid 2 at (19,10) in drowned_crypt.tmj, verified), on y=10 corridor, 5 from Sexton (21,5). Splits apse climb: (13,10) shoulder → (19,10) intermediate → (22,10) foot. Apse elite (16,4) r8 killed before Sexton aggros — elite kills 5 on three bots in gate #1.
- **Kiter band guard**: `frontRunnerOf(b).id == ownId` on map5 skips band. Runner owns route machine; banding it is r8c stall. Non-runners get far band 7-8 for casters (Gravemother bolt 6 vs Firebolt 8). Now exercised (r22 never reached map5, so was unmeasured) — gate #1 shows band firing, gate #2 never reached map5 so band not exercised there.
- **Speed**: gate #1 145-153s vs r21 232s — ~35% faster when it reaches. Variance: gate #2 0/5 never left map1 (traces 618/791 on map1, deepest map-3 x=36).

## 3. Remaining wall

When party reaches map5, it dies at font approach, last 7 tiles. Triple-aggro still: apse elite (16,4) r8 hp380, Sexton (21,5) r8 hp420, Gravemother (21,2) r8 hp700 dmg40. Deepest x in r21 was 15; r23 #1 didn't log deepest x but deaths near font.

Next moves, in order (same as T-125 §5, updated):

1. **Re-run r23 at 900s again** to confirm 5/5 is mode (n=2 at 900s: we have 5/5 then 0/5 — need n=2 more). Cheap, needed before claiming new leg of record.
2. **Break last 7 tiles**: options (a) extra node (22,6) or (19,6) — need walkable check (22,6 is near Sexton seat, may be blocked); (b) pre-mark ordering: kill apse elite (16,4) before crossing x=19; (c) allow band for runner at font only (routeIdx==4) — runner currently skips band everywhere on map5, but at font (22,3) it should kite.
3. **Boss verdict**: 5-deep party at font with bolts + staged crossing is config that can answer kill/TTK/unkillable. Not yet assembled — need to survive last 7 tiles first.

## 4. Environment (carry-over)

- `export PATH=$HOME/.local/bin:$PATH` every call; `ninja -C build/verify`.
- `ninja -C build/verify bh_maps` before tests/replay.
- Replay `bh_server --replay-world`.
- Sandbox wipes `.local/` and `build/` on restart — push early.
- Ground truth: epoch 21, wire 237, schema v11, suite 209/209 · 329,058, duel pin b273be661b54673a.
- Journals: r18-r22b, m3_gate (r17), t120, plus r23 (1/5) and r23b (0/5) as variance evidence.

## 5. Queue after T-126

- **T-127**: either r23 re-run (confirm mode) or extra node (22,6) or pre-mark ordering. Or pledge-lite sync (PR #22) — needs merge-wave authorization.
- Open PRs #22 (pledge-lite epoch22) and #23 (castle epoch23) still CONFLICT in tools/bots/main.cpp — need sync after this lands (this card touched same file).
- Next devlog 0088, next card T-127.

## 6. How to reproduce this card's legs

1. `rm -f build/m3_gate.db*` ; `bash tools/m3_gate_leg.sh 7907 15` to create 5 chars L1
2. `bash tools/m3_topup.sh` → L13 / gear
3. `bash tools/m3_gate_leg.sh 7908 300` → smoke, expect 5/5 at ~150s (but variance: may be 1/5), replay mm=0
4. `bash tools/m3_topup.sh` ; `bash tools/m3_gate_leg.sh 7909 900` → gate, expect 5/5 at ~150s (but variance: may be 0/5), replay mm=0
5. Check `logs/m3_gate_bots.log` for [raid] lines, `logs/m3_gate_server.log` for soak, replay line.
6. Leg variance is expected — run n=2 per config before verdict.

