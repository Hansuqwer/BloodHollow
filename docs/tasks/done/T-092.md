# T-092 — In-world refine overlays via trailing wire field (no version bump)

## Context
T-ART-11 shipped inventory-only glow because no entity snapshot carried refine (deliberate non-wire scope). This card finishes it: one trailing `u8 glowTier` on the entity spawn/delta path.

## Scope
- `shared/protocol/messages.md`: `u8 glowTier` appended to `EntitySpawn = 102` and `EntityDelta = 103` (0 none, 1 +5..9, 2 +10+). Build regenerates serializers; `kProtocolVersion` stays **237** (trailing-append count law, S16/T-070/T-071 precedent; fleet deploys lockstep).
- `server/src/world.{h,cpp}`: public `equippedGlowTier(e)` — equipped slot-0 refine maps to the tier both ends agree on (dormant 0-durability → 0, fists → 0, mobs → 0). Stale `InvSlot::refine` comment 0..3 → 0..7 (T-079).
- `server/src/main.cpp`: packed at all 3 sites (band respawn, AoI spawn, AoI delta — refine swaps/repairs ride the delta live).
- `client/src/net_client.{h,cpp}`: `NetEntSnapshot::glowTier` + unpack on spawn and delta.
- `client/src/game.cpp`: warm halo under glowing entities (alpha 70 law, mythic paler + wider, never brighter).
- No epoch bump (stays **13**): presentation-only field derived from existing deterministic state, unhashed (worldHash still id/zone/pos/hp) — proven by replay below, not just argued.

## Acceptance criteria
- [x] Codec round-trips the field on both messages.
- [x] Scan mirrors the client law on every branch (armed/unarmed/dormant/fists/mob).
- [x] Live bots converse cleanly with the new bytes (strict Reader!) + replay bit-exact.
- [x] Suite green; wire 237; epoch 13.

## Tests required
- `test_protogen.cpp`: glowTier round-trip subcases (spawn + delta). `test_refine_glow.cpp`: scan pins (7 checks). Suite 171→**172**, 328,619 assertions.

## Out of scope
- Overlay sprite frames (halo ellipse until art ships); new messages (would move 237).

## Evidence
- Suite: **172/172 (328,619 assertions)**, ctest 2/2, warning-free.
- Replay (no-bump proof): `logs/t091.bwj` (epoch 13, recorded pre-change) → OK 12801/7482/513 mm=0 on the new binary.
- Live smoke: 2 campaign bots, 60 s, `logs/t092_smoke.bwj` → OK 1801/223/73 mm=0 (29 kills, 0 deaths).
- Devlog: `docs/devlog/0061-wire-glow.md`.
