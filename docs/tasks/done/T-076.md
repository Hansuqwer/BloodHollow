# T-076 — Night base light: DIRECTOR OPTION (S36, DECISION)

## Context
GDD §9 (base light 6) vs shipped 0 (T-071). Shipped-wins would keep 0 —
but that guts the torch, so this is escalated, not implemented.

## Scope
- Devlog 0048 prices A (GDD-literal base 6: torch duration-only, epoch
  bump, fresh leg) vs B (keep 0, one-line GDD fix, zero code).
- Recommendation (non-binding): B pending playtest evidence.
- No code, no tests, no epoch change.

## Acceptance criteria
- [x] Both options priced with pointers; recommendation recorded.

## Out of scope
- Implementing either option (next sprint on director order).

## Evidence
- Devlog: `docs/devlog/0048-base-light-option.md`.
