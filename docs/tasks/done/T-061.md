# T-061 nightcreep — DONE S17 (2026-09-05)

## Context

The clock exists (T-008) but meant nothing yet. S17 gives the dark its
teeth: after 21:00 the mobs hunt differently.

## Scope

- `World::isNight()` via sim::hourAt: **21:00 → 05:00**.
- Mob swings: damage ×115/100 floored (min 1), one band, no stacking.
- Aggro radius effectively +1 tile at night (read-side, not written back —
  no morning downgrade churn of attack state).
- Not night-bound (deferred): "Blood Bolt +25%" needs the ranged-ability
  slot the Gravemother introduces (S18); noted as her signature there.

## Tests

- Band edges pinned: 20:59 no, 21:00 yes, 03:30 yes, 05:00 no (tick dial
  via debugSetTick seam).
- Bite law: paired seed-identical worlds (day dial vs night dial), player
  dex=0 so every mob swing lands — total damage ratio lands in (1.10, 1.20),
  i.e. exactly the ×1.15 siteline with per-hit floor noise contained.

## Evidence

- 4-case tests/test_night.cpp green; full suite **87/87** (327,479 assertions).
- Epoch stays 4: night hooks are pure multipliers (zero extra rng calls),
  so S16's epoch-4 journal still replays bit-exact; S17 leg replay OK
  `ticks=1292 hashes=12 mismatches=0` (logs/s17_smoke.bwj).
