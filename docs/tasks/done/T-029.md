# T-029 — Trade window (transactional) — DONE S7
Open trade with adjacent player (T), offer items/gold by intents, swap executes
only when BOTH commit. Validation at commit: ownership, quantities, gold,
distance ≤3; failure rolls back cleanly (nothing ever moves pre-commit).
Auto-cancel: partner leaves range/dies/disconnect-guard in tick. Tests:
atomic swap numbers, oversell rollback, distance auto-cancel (41 cases green).
Wire: proto v4 (30 msg): TradeOpen 14, TradeOfferItem 15, TradeOfferGold 16,
TradeCommit 17, TradeCancel 18. Client: T/P/X/H + inventory clicks + banner.
