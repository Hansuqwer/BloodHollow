# T-101 — Old Maw elite (fields; first of three, carries the announce)

## Context
T-097 options paper (Option A for Maw/Widow): mirror-1010 melee elites, fixed timers, broadcast first-kills, one card per elite. This card ships Old Maw + the shared first-blood announce the other two will reuse.

## Scope
- `shared/content/mobs.h`: row 1012 (L7, hp 145, dmg 19, def 12, xp 1200, Gnoll-base ×1010-pattern) + `namedEliteIdx()` helper (1012→0, 1013→1, 1014→2 — future slots reserved now so the helper never churns).
- `server/src/world.{h,cpp}`: session-scoped `namedEliteSlain_[3]` flags + first-blood broadcast (system channel, "X has fallen to Y — first blood") in `killMob` for player kills. Mob-on-mob elite violence is not a saga (players only).
- `tools/mapgen/make_fields_overflow.py`: `old_maw_pit` (28,38,6,4 — open south-center grass, derived+flagged) maxAlive 1, 36000t (30-min rotation). `.tmj` regen committed (only fields changes; generator truth).
- `docs/02-gdd.md`: Maw marked shipped + "random"→"rotating" (T-097 reword).
- Epoch **14→15** (new row + spawner + announce shift v14 journals). Old journals refuse exit 4 (verified).
- No wire change (stays 237 — no messages touched).

## Acceptance criteria
- [x] Row matches the paper (Gnoll ×1010-pattern, boss 0).
- [x] Pit spawns in fields_overflow (mapgen truth, spawner count 4→5 pinned).
- [x] First blood broadcasts once per reboot; second kill purse-only; mob kills silent.
- [x] Suite green; fresh epoch-15 soak + replay `mismatches=0`.

## Tests required
- `tests/test_elites.cpp`: table-law pins, pit-in-bhmap pins, announce-once pins. Suite 173→**176**, 328,649 assertions.

## Out of scope
- Red Widow / Cantor Vex rows+spawners (their cards); overlay art (hero fallback, 1011 precedent); EK-ledger persistence (session flag by design).

## Evidence
- Suite: **176/176 (328,649 assertions)**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 540 s, epoch-15 journal `logs/t101.bwj` → `[replay] OK ticks=12801 sessionCmds=6760 hashes=513 mismatches=0 entities=178`. Bots: campaign 0 / L3, fighter 54, pilgrim 46, wander 56 (healthy bands; Maw unengaged on map 2, stated).
- Refusal: `t100.bwj` (epoch 14) under epoch-15 build → exit 4.
- Devlog: `docs/devlog/0070-old-maw.md`.
