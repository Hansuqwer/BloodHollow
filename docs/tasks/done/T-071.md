# T-071 — Night light: torch + Blessed lantern + night-only spawns (S22b)

## Context
GDD §9, greenfield (no `lightRadius`/`torch`/`nightOnly` existed anywhere).
Day/night completion, second of two cards (with T-070).

## Scope
- **Server:** `Entity.lightRadius` (u8, 0). Torch (3003, 8g): use consumes
  → radius 6 for 300 s, exact tick edge. Lantern (3004, 150g): never
  consumed, toggles 8, never expires while held. Tick expiry. Potions/gold
  untouched. Marta stocks both (lantern price pinned overnight — flagged).
- **Client:** radial warm mask at night from lit AoI tiles, additive Effect
  (never below alpha-150 floor); panel F1–F7 Marta / F8–F10 fence.
- **Spawns:** `SpawnDef.nightOnly u8` + `.bhmap` v2 + mapconv prop; one
  `night_ghouls` spawner (36,26, maxAlive 4) in a quiet patch; refill +
  acquire gated to 21:00–05:00.
- **Epoch 9 → 10** + fresh leg replay.

## Acceptance criteria
- [x] Torch grants/expires at tick boundary; lantern toggle + persistence.
- [x] Mask constants pinned; floor-safe by construction.
- [x] nightOnly refill + aggro gating at the hour edge; bhmap round-trip.
- [x] Suite green; replay `mismatches=0` on fresh epoch-10 journal.

## Tests required
- `tests/test_light.cpp` (7 cases) + `test_bhmap` nightOnly pins.

## Out of scope
- New art; new vfx kinds; drop-table changes; L8→L9 step-up.

## Evidence
- Suite: **126 tests / 328,054 assertions**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 560 s, epoch-10 journal `logs/t071.bwj`
  (campaign 0 deaths, killerByLvl empty).
- Replay: `[replay] OK ticks=13201 sessionCmds=5633 hashes=133
  mismatches=0 entities=342`.
- Devlog: `docs/devlog/0034-night-light.md`.
