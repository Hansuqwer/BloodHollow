# T-066 flash/callout sweep — DONE S19 (2026-09-05)

## Context

HB's readability law: every system voice gets its own color/case lane so
the crowd can parse combat at isometric glance. Audit pass over what S13–S19
already had vs what was chatable-only.

## Scope

- Already lane-clean, kept: miss grey (0), hit base (1), crit "!" red-cap
  (2), SLAIN gold (3), XP +N green (4), Power-Swing red-caps (5), anvil
  teal tier ring (6), aura proc bands cleave/sunder/graft (7), Mend "mend +N"
  green (8), BLOOD BOLT crimson-violet (9).
- New events (server WorldEvent kind, CombatEvent-opaque field growth):
  10 BLESS (+10% gold over target), 11 IRONSKIN (steel-white over target),
  12 party-share shimmer (+N xp warm gold over each sharer).
- Death petrify-fade: client vestige queue (≤16) renders a dim grey
  silhouette at the departure pose for 0.6 s, sinking softly — no wire,
  harvested from despawnedIds before erase.

## Tests

- No testable math moved (client-side + event-shape only); full suite
  remains green to pin against the events the world emits.
- Kind numbering documented here as the single source of truth (grep for
  "cp.kind" in game.cpp).

## Evidence

- Suite **91/91** (327,815 assertions) — events added didn't disturb
  T-051/T-054 assertions.
- S19 smoke replay OK: `ticks=1182 hashes=11 mismatches=0` (floater events
  flow over CombatEvent wire live; worldHash untouched since nothing
  consumes rng on these paths).
