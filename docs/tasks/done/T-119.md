# T-119 — PR #18 merge wave (M3 gate lands as T-118) + PR pipeline filed  [DONE 2026-09-12]

## Context

Another session executed the continuation prompt's Track A1 (M3
party-crypt gate leg) and opened PR #18 — complete, CI-green, bots-only —
but branched from `c8d8087` carrying the original snippet's "next card
T-117" numbering. PR #17 (continuation-prompt filing) had consumed T-117 on
master in between: the repo's first cross-session counter collision, and
the PR arrived CONFLICTING. The director ordered: "make a prompt to merge
and Execute it. Then make a detailed Continue prompt for executing more
pr's."

## Scope (executed under `docs/prompts/merge-pr18-2026-09-12.md`)

- **Wave pre-record**: merge authorization prompt committed and pushed to
  `task/T-119-pr18-merge-wave` before any merge (T-116 pattern).
- **PR #18 sync + renumber**: merged master into `task/T-117-party-crypt-gate`
  (`e1a66da`); card `done/T-117.md` → `done/T-118.md`, devlog
  `0079-t117-crypt-gate.md` → `0080-t118-crypt-gate.md`, `logs/t117.bwj` →
  `logs/t118.bwj` (byte-identical; recorded `t117_*` bot names inside are
  history), script → `tools/t118_party_crypt_leg.sh` (paths made
  repo-relative — the only functional deviation); board junction resolved
  as T-117 filing row + T-118 M3 row in card order; PR retitled and the
  renumber documented in a PR comment.
- **Local re-verification of the synced branch**: offline build, suite
  329,022/329,022 assertions, duel pin `b273be661b54673a`, renamed leg
  replay `ticks=12042 sessionCmds=4634 hashes=120 mismatches=0` (matches
  the PR evidence exactly). CI green on the sync head, both matrices.
- **Merge**: PR #18 squash-merged as **T-118** (`cca3f94`); remote branch
  deleted (no children).
- **This filing**: the Continue prompt
  (`docs/prompts/continue-pr-pipeline-2026-09-12.md` — the repeatable
  card→PR→merge pipeline, including the collision rule worked from today's
  incident), this card, devlog 0081, board row.

## Outcome

Master at `cca3f94` carries the M3 gate verdict — **MET**: a 5-person
mixed-kit party (2 Ravager, 2 Cultist, 1 Gravecaller, L12 pre-seeded)
forms, traverses Thornwall → Crypt → Drowned Crypt, survives curse/slam,
and kills the Gravemother (2 kills in the 300 s leg, TTK < 15 s focused).
No tuning needed. Track A of the build queue is closed; next card T-120 =
weapon-skill persistence (Track B1).

## Acceptance criteria

- [x] PR #18 merged with the collision resolved by renumber, master's
      numbering preserved.
- [x] Renamed artifacts verified (replay of `t118.bwj` mm=0).
- [x] Continue prompt filed.
- [x] No code changes beyond renumbered comments + script path fix
      (documented); epoch stays 20, wire stays 237.
- [x] Master CI green, queue empty after.

## Tests required

Docs + bots-only wave: the synced branch's full battery (above) + CI on
both matrices were the gates.

## Out of scope

Any Track B work; tuning; further card numbers beyond T-119.

## Deviations

The sync also repaired the local sandbox environment (shallow repo
unshallowed; cmake bootstrapped via pip) — tooling only, no repo effect.
