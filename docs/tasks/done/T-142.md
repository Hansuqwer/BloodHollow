# T-142 — Player sheets to visible: wire class/sex + atlasFor + screenshots (Phase A, 2/3)

## Context

T-141 packed Ravager WIP sheets; the client still drew every player from the hero placeholder. `mobSheetPaths(0)` is pinned FALSE and `NetEntSnapshot` carried no class/sex — wiring needed a wire change, scoped here. Merge repair (2026-09-16, epoch 26→27) landed first: 11 files had committed conflict markers (H1/H2 mine+steward vs siege/pledge stack); resolved as union, fresh gate leg `logs/t146.bwj` (mm=0, guard refuses t140 exit 4).

## Scope

- Wire: `shared/protocol/messages.md` — `EntitySpawn` + `EntityDelta` gain `u8 classId` + `u8 sex` (0 unknown, 1 m, 2 f; class 1 Ravager / 2 Gravecaller / 3 Cultist per `kits.h`). No free byte in spawn/delta existed → bump `kProtocolVersion`. Protogen derived version `200+len(messages)` would not move on field addition, so `tools/protogen/protogen.py` base 200→201 → 237→238 (38→38 msgs, 37→38 via count now irrelevant — field bump dominates). Incompatible change: old clients get LoginResult reason 4.
- Server: `server/src/main.cpp` packs `m.classId = player ? e.classId : 0`, `m.sex = 0` (T-142b not yet captured — no sex source of truth exists in `Entity`/`CharacterRow`/creation flow) in both AoI spawn and delta-bridge paths; `d.classId/sex` ride delta so a `/kit` oath re-sheets remotes live.
- Client snapshot: `client/src/net_client.h` `NetEntSnapshot` +2 fields (0 defaults); `net_client.cpp` copies them spawn/delta with `kind==0` gate (mobs 0).
- Atlas law: `engine/assets/atlas.h` `playerSheetPaths(classId, sex)` (classSlug `ravager|gravecaller|cultist`, sexSlug `m|f`) → `assets/aigen/players/<class>/<sex>/sheet.{png,json}`. Returns false on `sex==0` / unknown class. `client/src/game.h` per-(class,sex) cache `playerAtlases_` (`key = class<<8|sex`) + `atlasForPlayer`. `game.cpp` `drawRemoteEnt` branches `kind==0 ? atlasForPlayer : atlasFor`; unknown/missing sheet falls back to `heroAtlas_` (same contract as mob 1011).
- Tests: `tests/test_clientlaw.cpp` `T-142` pins ravager m/f resolve, gravecaller/cultist resolve (sheets not yet shipped — loader fallback still true), unknown/sex0 false.
- No epoch bump (render-only, snapshot-only; proven by `t146.bwj` mm=0). No sim change.

## Acceptance criteria

- Own + remote players render from shipped Ravager sheets on a live server (Gravecaller/Cultist fallback to hero until sheets land); fallback on unknown class/sex; suite + validate green; wire 238.

## Tests required

- clientlaw pins above; loader-cache headless-untestable (raylib), not faked. Suite 299/299 (was 298), 2,330,208 assertions, ctest 2/2, validate 0/6.

## Out of scope

- T-142b sex capture at creation (schema + creation UI + persist + journal `k`-line) — sex rides 0 until then. R-LUMA contrast remediation (day Δ10.7/night Δ4.3 vs gate 25/15): filed, not silently restyled. Gravecaller/Cultist art plates (probe-before-batch, 10/turn cap).

## Close-out

- Wire `kProtocolVersion` 237→238 (base 201). Server sex 0 until T-142b (CharacterRow has no sex column — creation must capture it; scope alert filed). Client `atlasForPlayer` lazy-caches; hero fallback covers sex0/unknown and missing Gravecaller/Cultist sheets.

## Deviations

- Wire bump was required (no free byte); protogen base bump documents the field-addition class. Sex=0 interim is a deferred schema gap (T-142b).

## Evidence

- Build: `cmake --preset linux-gcc && cmake --build --preset linux-gcc -j4` warning-free. `bh_tests` 299/299, `ctest 2/2`, `validate_links 0/6`. Wire 238 `build/generated/protocol/messages_gen.h:14`. Merge-repair leg `logs/t146.bwj` mm=0 (epoch 27).
