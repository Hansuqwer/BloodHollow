# T-124 — PR #25 merge wave: the r8→r17 gate series lands, #24 closed superseded  [DONE 2026-09-13]

## Context

The director directed the merge of **PR #25** (`arena/01a09acb-bloodhollow`,
head `9163617`) — the T-118 M3 gate raider series, r8→r17 — with the
instruction that this overrides the standing never-self-merge default, and
that **PR #24** (the older r8 lineage, head `arena/01a096c5-bloodhollow`) be
closed as superseded rather than merged. Repo convention is squash (devlog
0083: PR #19 squash-merged as `7b2e9b8`).

Pre-wave state, verified rather than assumed:

- master at `818d661` (unchanged since PR #21); PR #25 **MERGEABLE / CLEAN**,
  `statusCheckRollup` = 4× `COMPLETED/SUCCESS` (build-test on ubuntu-latest
  and macos-latest), 14 commits, base an ancestor of the head — no rebase
  needed, so the local-battery-on-the-PR-branch step was not triggered.
- PR #25's diff touches **only** `tools/`, `docs/`, `logs/`
  (`git diff --stat` over `server shared client engine tests` is empty) —
  no sim semantics, no wire, no schema.
- PR #24: 4 commits, `mergeable=CONFLICTING`, `mergeStateStatus=DIRTY`.

## Scope

- **Merge PR #25** with `gh pr merge 25 --squash`, subject = the PR title
  (`T-118: M3 gate — r8→r17 raider series (font reached once; boss verdict
  not established) (#25)` — the `(#NN)` suffix matches master's existing
  squash subjects), body = the PR body. Landed as
  **`2a813ec6658d567956acb386343bf89b780be47a`**.
- **Close PR #24** unmerged with a comment recording the supersession (its
  four r8 commits are cherry-picked at the base of #25's series), the
  CONFLICTING state, and the series verdict. Comment:
  `pull/24#issuecomment-5655374754`.
- **Post-merge verification on master** (see Tests required).
- **This filing**: devlog `0085`, board row, dated amendment to
  `docs/prompts/continue-pr-pipeline-2026-09-12.md` §5.

Card number: the board's highest done card on master was T-121, but **T-122
and T-123 are claimed in flight** by PRs #22 and #23, so this wave took
**T-124** rather than force a large feature PR to renumber.

## Outcome

Master `2a813ec` carries the full raider series: the `raider` bot profile
(one-brain column, direction-aware runner election with dead-band 2,
pile-node wait-immunity, guarded desync re-adoption, TEMP trace scaffolding
stripped), the harness trio (`m3_gate_leg.sh` / `m3_grind_leg.sh` /
`m3_grind_chain.sh`) with the disclosed `m3_topup.sh` staging, the journal of
record `logs/m3_gate.bwj`, the r17-era handover with its do-not-retry list,
devlog 0084 and the card `done/T-118-m3-gate-raider-series.md`.

**Verdict on record:** hard gate (reach the font) **PASSED once** — leg r9,
3/5 bots into map 5 (692.9 / 692.5 / 617.1 s), boss sighted, clock expired
before engagement. **Boss verdict NOT established** — no leg ever engaged the
Gravemother. The blocker is content-side (map-3 respawn-swarm attrition), not
bot posture; r14 restored r9's posture and did not reproduce the reach, so
r9 is treated as luck-dependent. Follow-on options are
`docs/handover/T-118-r17.md` §5; §4 is the do-not-retry list.

Ground truth unchanged: journal epoch **21**, wire **237**, schema **v11**,
duel pin `b273be661b54673a`.

## Acceptance criteria

- [x] PR #25 state re-checked immediately before merging
      (MERGEABLE/CLEAN, both matrices green, master unmoved).
- [x] Squash-merged with the PR title as subject; `mergedAt`
      2026-09-13T18:56:46Z, `mergeCommit` `2a813ec`.
- [x] PR #24 closed unmerged with a supersession comment; not merged.
- [x] Full battery re-run on merged master (tests + duel pin + journal
      replay + epoch guard) — numbers in Tests required.
- [x] Devlog 0085 + board row + pipeline-prompt amendment filed.
- [x] In-flight queue reported honestly: PRs #22/#23 now conflict in
      `tools/bots/main.cpp` and carry devlog numbers master has taken.

## Tests required

Post-merge on `2a813ec`, headless preset (`-DBH_BUILD_CLIENT=OFF` — apt is
blocked in this sandbox so raylib cannot build; the T-108 gate excludes the
three client-law TUs, 7 `TEST_CASE`s, which CI's client-ON matrix covers):

```
ninja -C build/verify bh_bots bh_server bh_tests bh_duel   # 74/74 clean
ninja -C build/verify bh_maps                              # required: assets/maps/*.bhmap
./build/verify/tests/bh_tests       # 209/209 · 329,052 assertions · 0 failed
ctest --test-dir build/verify       # 2/2 passed
./build/verify/tools/duel/bh_duel --selftest
                                    # win=1 ticks=41 hpEnd=82 hash=b273be661b54673a
./build/verify/server/bh_server --replay-world logs/m3_gate.bwj
   # [replay] OK ticks=36042 sessionCmds=5933 hashes=360 mismatches=0 entities=189
./build/verify/server/bh_server --replay-world logs/t120.bwj
   # [replay] OK ticks=1500 sessionCmds=529 hashes=15 mismatches=0
./build/verify/server/bh_server --replay-world logs/t118.bwj
   # refused: journal epoch 20 vs build epoch 21, exit 4 (by contract)
```

The epoch guard did **not** refuse `m3_gate.bwj` (journal 21 vs build 21) —
the series never moved the epoch, so the merged journal replays clean on
master.

## Out of scope

Merging PRs #22/#23 (in flight, now conflicting — a sync merge, not this
wave); deleting the merged `arena/01a09acb-bloodhollow` remote branch; any
T-118 follow-on leg (r18 / content-side map-3 fixes) — those need their own
card; any epoch, schema or wire change.

## Deviations

- The wave ran on the session-pinned branch `arena/01a09c1b-bloodhollow`,
  fast-forwarded to the new master; the merged branch
  `arena/01a09acb-bloodhollow` was **not** pushed to and **not** deleted
  (prior waves deleted merged branches; that is left to the director here
  because this session is pinned elsewhere).
- Verification used the headless preset instead of CI's client-ON
  configuration, for the reason above. Test/assert counts match the board's
  pin exactly, so nothing is hidden by the substitution.
