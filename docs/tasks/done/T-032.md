# T-032 — World journal record/replay (M2 deterministic wipe repro) — DONE S7
Server `--record-world FILE.bwj` logs l=login+c=bungeatable commands+d=disconnect
+h=every-100t worldHash markers. `--replay-world FILE.bwj` (offline, no net)
rebuilds the world step-for-step on the fixed seed and verifies every marker.
Verification artifact: `logs/2026-09-04-journal-verify.bwj` → replay
`OK ticks=801 sessionCmds=308 hashes=9 mismatches=0 entities=40`.
Off-by-once found & fixed: login timing (parse-time vs recorded tick) and
disconnect despawns were both previously invisible to the journal.
