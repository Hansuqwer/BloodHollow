# T-112 — Epoch follow-up for F4 spawn fill (verified 2026-09-17)

## Context

T-111 claimed no epoch bump but F4 bounded scatter changes boot spawn counts + RNG stream (t107 81/81 mismatches). T-112 bumped `kJournalEpoch` 18→19 with 3 spawn-law pins. Handover flagged card still open, but code is in tree (epoch now 27 supersedes).

## Verification 2026-09-17

- `kJournalEpoch` is 27 (`server/src/main.cpp:123`), history line documents 18→19.
- `tests/test_t112_spawn_epoch.cpp` 3 cases PASS (F4a fully-blocked terminates, F4b half-blocked fills, F4c single-tile fills). Suite 299 includes them.
- `t112.bwj` leg was superseded by `t146.bwj` (epoch 27 entity-set shift), but `t146` mm=0 proves 27.
- No code change needed — already shipped; epoch 27 is the current truth.

## Close-out

Card moved `tasks/T-112.md` → `done/T-112.md`. No wire/epoch change.

## Deviations

- None.
