# T-002 — Window/input + fixed-tick harness (S1)  [DONE 2026-09-04]

Scope: raylib window, `--vsync`, `--map`, `core/timestep.h` accumulator with
spiral-of-death guard; sim runs only in whole 20 Hz ticks.

Evidence: client main loop; scripted demo run reached tick 35 while frames
free-ran (accumulator working as intended).
