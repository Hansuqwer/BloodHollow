# T-091 — Gravemother telegraphed slam (epoch 12→13)

## Context
GDD boss line: Gravemother with "ground rot telegraphs". T-ART-09 shipped `addTelegraph`/`addCircle` with no callers. This card gives the Mother her signature: the instant Blood Bolt becomes a 60t wind-up + radius-2 slam. Gravecaller keeps the instant bolt (shared path preserved).

## Scope
- `server/src/world.{h,cpp}`: transient wind-up state (`slamAt/slamX/slamY`, never persisted — mobs respawn fresh; zero new RNG draws, replay reproduces from cooldown + positions). Wind-up arms on the shared bolt cadence (telegraph tax, flagged); boss holds still while winding (readable); strike hits all players in chebyshev 2 of the recorded tile with bolt-mirror numbers (dmg stat, half-plate, night +25%, min 1, T-070 curse lane on survivors, per-victim kind-16 events); movers dodge. Death/leash/mark-loss fizzles the fuse (killPlayer clears attackers' fuses + think-top dud guard).
- `engine/render/decals.h`: one wind-up event stages 1-2-3 by age (shared 60t clock) + 80t visible window (20t scorch past the strike).
- `client/src/game.cpp`: kind-15 harvests telegraph decals + gold "!" floater + boss cast anim; kind-16 red "SLAM" floater + hurt anim. Unknown-kind default (numeric red) untouched for future kinds.
- Epoch **12→13** (content-sim change: new boss outcomes + new journaled event kinds). Old journals refuse cleanly (exit 4 — verified, not a failure).
- GDD boss line updated with slam specifics (shipped-wins: design already named telegraphs).

## Acceptance criteria
- [x] Wind-up announces (kind 15) with zero damage during fuse.
- [x] Stayers eat rot + curse; movers dodge; mark-loss fizzles.
- [x] Suite green; fresh epoch-13 soak + replay `mismatches=0`; epoch-12 journal refuses exit 4.
- [x] Gravecaller bolt untouched (shared path).

## Tests required
- `tests/test_slam.cpp`: arm-silence pins, hit/curse vs dodge pins, cancel pins. `test_decals.cpp`: staging/visibility/window pins. Suite 167→**171**, 328,601 assertions.

## Out of scope
- Phases 2–3, bell adds, Sanctuary/Wither circles; Gravecaller changes; in-world overlay sprites (task 3 owns the wire field).

## Evidence
- Suite: **171/171 (328,601 assertions)**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 540 s, epoch-13 journal `logs/t091.bwj` → `[replay] OK ticks=12801 sessionCmds=7482 hashes=513 mismatches=0 entities=360`. Bots: campaign 0 deaths / L4, fighter 54, pilgrim 28, wander 88 (mid-band roam-RNG per T-083; no L11 clustering, no perch).
- Bands note (T-087 discipline): Ghoul 37.2 s (n=32) sits outside the 8–25 s band with n≥30 — checked, not a claim: no T-091 code path touches ghoul combat (slam is 1009-only, map 5; this leg is map-1), same wobble family as the 16-leg series (up to 177 s). Noise verdict stands.
- Refusal: `t084.bwj` (epoch 12) under epoch-13 build → exit 4 with contract message.
- Devlog: `docs/devlog/0060-telegraph-slam.md`.
