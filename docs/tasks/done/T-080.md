# T-080 — Trade transaction log (S33)

## Context
GDD §7 server-side transaction log for every trade (dupe audits).

## Scope
- One canonical audit line per executed swap; nothing otherwise.
- No epoch bump (stays 12) + short soak + replay.

## Acceptance criteria
- [x] Line shape pinned; cancel/oversell append nothing.
- [x] Suite green; replay `mismatches=0` on fresh epoch-12 journal.

## Tests required
- `tests/test_tradelog.cpp` (2 cases, tmp-path seam).

## Out of scope
- Analytics/dupe-hunting UI; dated filenames (fixed path, flagged).

## Evidence
- Suite: **154 tests / 328,457 assertions**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 540 s, epoch-12 journal `logs/t080.bwj`
  (campaign 0 deaths, killerByLvl empty).
- Replay: `[replay] OK ticks=12801 sessionCmds=7389 hashes=129
  mismatches=0 entities=354`.
- Devlog: `docs/devlog/0045-trade-log.md`.
