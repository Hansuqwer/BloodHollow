# T-141 — Ravager WIP sheets: pack + QA + triage (Phase A, 1/3)

## Context

20 landed Ravager keyframes (walk-f0/die-f3 S/SE/E, attack-f1/cast-f2 SE/E
× m/f, flux-accepted with Runs) sat as plates+cells with no sheet. Phase A
packs them to in-client sheets per the frozen pipeline.

## Scope

- `bh_pack_sheet.py` (32×48, anchorY 42, walk/attack/cast/die,
  `--pad-missing`, ≤32 colours) → `sheet.png` + `sheet.json` (v1) for
  ravager m/f. Sheet is explicitly WIP: idle/hurt/gib + W-side padded.
- `bh_qa_sheet.py` triptychs + audits → `docs/research-notes/qa/b6_*`.
- Triage: m `walk_SE` floater fixed (+4px in-cell shift, feet now on
  anchor); R-LUMA day/night FAIL (Δ 10.7/4.3 vs 25/15 — muddy mid-tones)
  recorded, remediation filed as T-142.
- `assets/LICENSES.md` rows (plates + sheets + QA).
- OUT (T-142): atlasFor wiring + in-engine screenshots; contrast
  remediation; Gravecaller/Cultist plates (not in repo — generation turns).

## Acceptance criteria

- Sheets pack clean (colour gate true); QA audits on file; b5_build.sh
  exit 0; suite + validate green (content-only: no epoch, no wire, no code).

## Tests required

- None (no code). Evidence is the QA audits + pack reports.

## Deviations

- None.
