# T-037 — Client zone handoff — DONE S8 (code-complete)
Welcome is reused as ZoneTransfer: server's zoneChanged event clears the session
interest set and resends Welcome(mapId,x,y)+OwnStats+Inventory. Client watches
net.mapId vs loadedMapId in frameBegin => Game::zoneReload() hot-swaps the
bhmap (per-id file table), clears remote ents/floaters/target, snaps the camera
to the arrival tile. Bots reset their view the same way (Welcome re-receipt).
