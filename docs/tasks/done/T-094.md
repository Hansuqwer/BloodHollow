# T-094 — Thornwall NPC posts + one-anvil fix (epoch 13→14)

## Context
T-ART-06 engine side shipped constants + seams; sheets/portraits need art. This card puts the Thornwall bodies on the map: twins flank the anvil, Ashen/Synod guards stand the T-073 posts. Registrar (72) and steward (73) stay unplaced — Marrowgate and the Weeping Castle do not exist. Positions derive from the brief + existing posts (flagged for art review, T-075 derivation precedent).

## Scope
- `server/src/world.{h,cpp}`: `spawnNpcs()` (zone 1, after anvils) — twins on the first two free tiles spiralling from the anvil; Ashen spiralling from east-gate (60,13), Synod from bridge (13,31); occupied-furniture skip, deterministic, no RNG.
- **One-anvil fix (found live during the sprint):** `spawnAnvils` seeded an anvil per ring row (~63/zone — the exact T-069 Marta failure mode, bare `break` leaving the dx loop). One anvil per zone now (Marta-pattern placed flag). Same epoch covers it (entity-count content change).
- Epoch **13→14** (live furniture added/removed under v13 journals). Old journals refuse exit 4 (verified).
- No wire change (stays 237), no mapgen change (runtime spawns, generator untouched).

## Acceptance criteria
- [x] Twins ×2 + Synod + Ashen place live with right kinds/names (arena sweep pins twins/Synod; Ashen pinned via seam — gate tile off-arena, stated).
- [x] Anvil count pinned at 1/zone (the 63-anvil state can never recur — sweep asserts).
- [x] Karma/vendor/fence lanes untouched (proximity-keyed, counts-independent).
- [x] Suite green; fresh epoch-14 soak + replay `mismatches=0`.

## Tests required
- `tests/test_npcs.cpp`: live-placement sweep (twins 2, Synod 1, Ashen 0-off-arena, anvil anchor present). Suite 172→**173**, 328,624 assertions.

## Out of scope
- 72/73 placement, sheets, portraits, dialogue, guard behavior (furniture posts until a behavior card); EK ledger; named elites.

## Evidence
- Suite: **173/173 (328,624 assertions)**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 540 s, epoch-14 journal `logs/t094.bwj` → `[replay] OK ticks=12801 sessionCmds=7240 hashes=513 mismatches=0 entities=180`. Bots: campaign 4 / L3, fighter 34, pilgrim 36, wander 56 (healthy roam-RNG band).
- Entity dividend: live count **~360 → ~180** (anvil spam was half the world); p50 ~0.7–0.9 ms / p99 **~1.3–1.6 ms** (was 3.8–5.9). M5 budget headroom doubled.
- Refusal: `t091.bwj` (epoch 13) under epoch-14 build → exit 4.
- Devlog: `docs/devlog/0063-thornwall-posts.md`.
