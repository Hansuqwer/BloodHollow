# T-143 — Ops to alpha, agent-closable boxes (Phase O, 1/2)

## Context

MVP §7 boxes triaged 2026-09-15: launcher/site/clean-boot/surveys/mac-human
are director-owned (stop triggers, NOT started). This card closes what an
agent can verify: unit, backup drill, runbook, crash-reporting posture.

## Scope

- `tools/ops/bh-server.service` (auto-restart, journal logging, resource
  guardrails, hardening; `systemd-analyze verify` clean).
- `tools/ops/bh_backup.sh` (live `.backup` + rotation + manifest +
  restore + `drill` verb) + DRILLED evidence (not just written):
  t140 leg DB → backup ok → restored `accounts=5 chars=5 pledges=1 uv=14`,
  integrity ok.
- `docs/ops/gm-runbook.md`: kick/ban/rollback/siege/announce mapped to
  VERIFIED verbs only; gaps carded, not invented (`/ban` missing,
  free-text broadcast missing — workaround documented).
- Crash reporting = journald + coredumpctl weekly (no in-process handler
  by design — documented why).
- Handshake refusal already in-protocol (Hello.protoVersion → reason=4);
  launcher remains the open box that consumes it.

## Acceptance criteria

- Unit verifies, drill passes on a real leg DB, runbook cites only real
  verbs, suite + validate green (no code touched).

## Tests required

- None (no code). Evidence is the drill output + verify output.

## Deviations

- None. Box score after this card: 3/7 agent boxes closed (unit, backup
  drill, runbook); crash-reporting posture set; launcher/site/legal/
  clean-boot stay director-owned.
