# T-004 — Atlas/anim loader + placeholder hero (S1)  [DONE 2026-09-04]

Scope: uniform-grid atlas format (8 dir rows x N frames) + `animFrame()`
selection; procedural `makeHeroAtlas()` (8 dirs x 6 walk + idle, 32x48 px,
hooded figure with direction-facing face pip, leg swing, shadow) so the repo
needs zero binary art at M0.

Evidence: hero renders + walks in m0-day.png; real art slots in via the same
JSON schema without code changes.
