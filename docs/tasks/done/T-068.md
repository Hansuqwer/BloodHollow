# T-068 — L8 content gate: move the gravecaller barricade off the bridge approach [DONE 2026-09-07]

## Context

Devlog 0029 closed with a flagged scope boundary: L7→L8 has no reachable
step-up on map 1. `gnolls_pits` (L7, x[50,57] y[42,45]) and `widow_glade`
(L9) sit south of the river (`WATER y[34,37]`), and the only crossing —
bridge `PATH x[14,16] y[34,37]` — was flanked by the L11 Gravecaller
barricade (rect (6,18) 5×4, maxAlive 3, aggro 7, dmg 30, leash 12): the
gravecaller kill zone (leash 12 Chebyshev around the anchor) covered the
south road `x[14,15] y[17,33]` that feeds the bridge. Bot tuning was
exhausted (Route v5c, devlog 0029); this was flagged as a **content**
decision for the director.

Director decision (2026-09-07): **Option A — move the barricade off the
bridge approach** (over options B "add a crossing" and C "re-anchor the
gnolls").

## Change

1. `tools/mapgen/make_thornwall.py`: `gravecaller_barricade` rect
   `(6,18,5,4)` → `(1,43,1,3)`. Placement derived from the threat model
   (spawn anchor is a random in-rect tile, `world.cpp:228`; kill zone =
   leash 12 around the anchor; aggro fires ≤7 from the mob, +1 at night):
   even the worst in-rect anchor leaves mob reach (wander 5 + aggro 7)
   **≥ 8 tiles from every bot-route tile, day and night** — south road,
   bridge columns, east-bank diagonal, east road, town. The L11 stays as a
   far-marsh hazard, not a roadblock. Map regenerated
   (`data/maps-src/thornwall.tmj`, spawner count 10, walkable 83.9% —
   unchanged apart from the one spawner line).
2. `server/src/main.cpp`: journal epoch **6 → 7** — a moved spawner shifts
   the sim under v6 journals; stale by contract, not broken (same response
   as T-030 content drop and the T-034b retune). Verified the contract
   fires: the replay runner refuses a v6 journal with the plain-language
   refusal and asks for a fresh gate leg.
3. `tools/bots/main.cpp` (campaign profile): L7+ waypoint opens —
   `campX/campY = (53,41)`, the **gnolls north edge**. Gnolls (wander 8,
   aggro 7) pull straight onto the camp; road ghouls (L3) are fodder via
   the defend branch; retreat-home now crosses the river north instead of
   into worse content. `widow_glade` (L9) shares the bank and may pull —
   pack cap + retreat-home handle it. This waypoint is the verification
   vehicle for the content change; without it `TARGET L8` can never fire.

## Acceptance criteria

- Build warning-free; ctest 2/2; bh_tests 105/327,925.
- Journal epoch bumped with documented chain; old journals refused by
  contract; fresh leg replays 0 mismatches.
- L6-resumed val run: bots route south through the bridge to the gnolls
  camp with **no L11 deaths** (killerByLvl free of 1007/L11), and reach
  L7; L8 probed.

## Evidence

- Build warning-free (`-Werror`); ctest 2/2; bh_tests **105/105 (327,925
  assertions)**. Map regenerated: `make_thornwall: 64x48, walkable 83.9%,
  spawners 10, portals 2` (unchanged apart from the one spawner line;
  CI's generator-diff gate satisfied by committing the regenerated `.tmj`).
- Epoch contract: old v6 journal refused by the replay runner with the
  plain-language refusal (`journal epoch 6 vs build epoch 7 — sim semantics
  changed since; record a fresh gate leg`).
- val5b (chain DB, chars set L7 in town): **`TARGET L8 DONE in 444.3s`**,
  peak L8 / end L7, deaths 6 (zero L11), killerByLvl L2:2+L5:2 only,
  replay `OK ticks=15601 sessionCmds=1884 hashes=157 mismatches=0`.
  Journal `logs/val5b.bwj` committed as evidence; devlog 0030.
- val5 (DB resumed as-is, chars parked at the perch): 156 deaths,
  killerByLvl L3:76/80 — the known bad-leg death-loop mode (devlog 0009
  precedent; chain legs 3/5), a harness-state artifact, recorded as a
  diagnostic in devlog 0030 with the sqlite-set-state lesson.

## Out of scope

- L8→L9 (`widow_glade`) and L9+ step-ups; widow/barricade adjacency tuning
  if the L8 probe shows a widow wall (follow-up card with measured data).
- Any mob number changes (none: same mob, same maxAlive/respawn — only the
  rect moved).
