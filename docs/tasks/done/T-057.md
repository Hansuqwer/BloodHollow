# T-057 alignment chrome — DONE S15 (2026-09-05)

## Context

The gamble must be visible on the character sheet and on the crowd
(readability pin: era red-names + our own red-caps rule). The wire knows
karma only for self (OwnStats); crowds need a band on spawn.

## Scope

- Wire: `EntitySpawn` gains `u8 karmaBand` (0 lawful >500 / 1 neutral 0..500 /
  2 chaotic <0) — field growth, version is count-derived (stays v237).
- Mid-session band crossing (karma crossing 0 or 501): server flags
  `statsChanged` (self panel) AND re-sends `EntitySpawn` for the aboutId to
  every online session (cheap upsert; era crowd notice) + ch-255 line
  "X's soul blackens." / "X's soul whitens.".
- Client: nameplate text red when band==2; stat panel adds one legibility
  line: `alignment: lawful|neutral|CHAOTIC (karma N)` with the stakes gloss
  ("red = drops on death, shops refuse") when chaotic.

## Tests

Band function edges (-1/0/500/501/1000); crossing fires the event exactly
once per crossing (both directions).


## Evidence

- Band edges pinned (-1/0/500/501/1000) + single crossing event each way.
- EntitySpawn + karmaBand (v237 field growth); band-crossing pushes an AoI
  re-spawn (crowd sees the name redden live) + ch-255 soul line.
- Client: nameplates red (chaotic) / pale-blue (lawful); stat panel stakes
  line ("red: drops, no shops").
- Smoke leg wire-compat v237 client↔server verified by the replay above.
