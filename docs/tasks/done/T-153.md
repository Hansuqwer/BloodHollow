# T-153 — Real password hashing argon2id (DONE 2026-09-16)

Source draft: `docs/tasks/T-153-argon2id-auth.md` (removed on completion —
content in git history). ADR-0012 filed BEFORE code (in this branch).

## Shipped

- Vendored argon2 20190702 reference impl, pinned tarball
  SHA256 `daf972a8...` (full hash in `third_party/CMakeLists.txt`), built
  directly as `bh_argon2` (argon2/core/ref/thread/blake2b/encoding; CC0/Apache
  dual, T-148 manifest note in ADR). Single `thread.c` (no thread-pthread.c
  in this upstream tag — caught before build).
- Params m=19456 KiB / t=2 / p=1, PHC string storage. Measured this box:
  **hash=47 ms, verify=44 ms** (test MESSAGE line).
- Schema v16: `accounts.pwhash_phc TEXT` (fresh DBs migrate 0→16; old rows '').
- Verify: PHC → `argon2id_verify` (constant-time in lib); empty → stub with
  branch-free u64 compare → **rehash-on-login** (best-effort UPDATE, login
  succeeds regardless). Garbage PHC → refused reason 1, no crash.
- New accounts hash argon2id at birth (stub columns ride for continuity).
- Threading (honest): single-threaded server — each verify burns ~one hash
  inside the 250 ms tick resync budget; T-109 limiter spreads bursts. Reason
  codes preserved (lockout accounting untouched).
- Runbook §7: wave-1 `--no-register` default documented.

## Tests

5 new (`test_t153_auth.cpp`): v16 migration, PHC-shape + wrong-pw refusal,
stub→argon migration incl. second-login-uses-argon proof (stub corrupted),
malformed-hash refusal, cost bound (<2 s, prints ms). Suite 330/330.
Latency: 44 ms verify stated; 30-bot wave number deferred to a live soak
(limiter, not hashing, dominates — stated, not measured).

## Out of scope (kept)

Off-thread auth pool (card if p99 demands), password reset/2FA/merge (per
draft), true pre-v16 file migration test (wipe-simulation covers the logic).
