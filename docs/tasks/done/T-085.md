# T-085 — Base-light verdict: Option B (docs-only)

## Context
T-076 (S36) priced A (GDD-literal base 6, torch duration-only, epoch bump) vs B (keep shipped 0, one-line GDD fix) with non-binding recommendation B. Director ordered execution in the 1–10 run: implement the verdict.

## Scope
- Option B: one GDD §9 line — "base 6" becomes "base 0; torches 6, lantern 8". Zero code. Torch keeps its 8g-sink value proposition; lantern still +2 and infinite; mask constants unchanged.
- No sim change → no epoch bump (stays 12), no wire change (stays 237), no soak journal needed (nothing replays differently — proven by the untouched replay line below).

## Acceptance criteria
- [x] GDD §9 matches shipped (base 0).
- [x] Suite green; standard replay still `mismatches=0` (no behavior change).
- [x] T-076 DECISION closed as executed.

## Tests required
- Replay of an existing epoch-12 journal (no new journal — nothing to record).

## Out of scope
- Option A (base-6 code, torch duration-only, epoch 13) — parked unless night-exploration playtests report darkness fatigue.
- T-ART-03 phase-2 additive mask (separate future layer).

## Evidence
- GDD diff: `docs/02-gdd.md` §9 night line.
- Replay: `./build/server/bh_server --replay-world logs/t082.bwj` → `[replay] OK ticks=12801 sessionCmds=7391 hashes=129 mismatches=0` (unchanged binary, unchanged journal).
- Suite: 160/160 (328,489 assertions), ctest 2/2.
- Devlog: `docs/devlog/0051-base-light-verdict.md`.
