# T-087 — Mid-band TTK wobble: deterministic core holds, soak metric is regime noise (CLOSE)

## Context
Standing watch since T-069: Ghoul TTK readings swing 8.5–177.6 s across soak legs against the T-033 8.9 s pin, twice touching the pin (T-081 8.5, "noise confirmed, not closed"). T-074 discipline owns the comparison — this card runs it.

## Scope
- Regenerate the gear-aware duel table twice + diff (determinism proof), pin the standard Ghoul matchup, blame the Ghoul content row, collect the 16-leg soak series. No code change (nothing to fix). No epoch bump (stays 12).

## Findings

- **Deterministic core:** `bh_duel --selftest` → DETERMINISTIC; `--table` twice → byte-identical (`logs/duel-table-t087.csv`, 139 rows). Standard Ghoul (L3, L3, fists): **9.7 s med / 11.2 p90**; shank 5.7 s; Pit Blade 4.8 s; geared + skill + vials 0.8–1.6 s.
- **Content untouched:** Ghoul row (`1002, hp 64, dmg 9, def 5`) last modified Sprint 18 (`a6635e4`); T-034b retune moved widow/gnoll only. No combat-math change in the entire wobble window.
- **Soak series (16 legs):** 11.3, 13.2, 59.3, 177.6, 24.5, 58.1, 22.8, 164.5, 24.7, 13.2, 15.0, 8.5, 12.8, 37.5, 24.3, 74.7 (n = 10–41). The metric (`TTK(25t hps)`) folds pursuit, kiting, pack chaos, and killer mix (L1 fists vs L8 geared) into one number — duel's clean 1v1 runs 3–10× faster by construction. Outliers (59–177 s) ride small-n legs with odd mixes (e.g. t084: L8 campaigners deprioritizing ghouls, n=10).

## Acceptance criteria
- [x] Duel table regenerated + determinism proven.
- [x] Wobble attributed with numbers (regime noise, not sim drift).
- [x] Pin updated (below); watch closed.

## Tests required
- `bh_duel --selftest` (ctest gate, green) + table diff. Suite 165/165 untouched.

## Out of scope
- Re-tuning Ghoul (or anything) to chase the metric — explicitly rejected.

## Evidence
- `logs/duel-table-t087.csv` (139 rows, twice-generated identical).
- Suite: 165/165 (328,518 assertions), ctest 2/2.
- **Re-pin:** Ghoul standard (L3 vs L3, fists) = 9.7 s duel-med; soak band 8–25 s at n≥20. Readings outside the band need n≥30 + killer-mix table before any claim (T-074 discipline). Watch CLOSED.
- Devlog: `docs/devlog/0055-ttk-closure.md`.
