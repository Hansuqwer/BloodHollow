# T-157 — Evidence refresh at epoch 29 (VERDICT 2026-09-16: M4 FAIL, cause found, fixes carded)

## m4e29 run (this wave)

`BH_BUILD_DIR=build/headless bash tools/t137_m4_leg.sh 7849 12 6 750 m4e29` @ epoch 29:
`logs/t137_m4e29.bwj` (48 KB, force-added — replay anchor).

| Check | Result |
|---|---|
| battles (`battle joined`) | **0** |
| gates-down (`breached by`) | **0** |
| attuned | **0** |
| flips (`crowned:`) | **0 → M4 FAIL** |
| p99 max | 2992 µs (3.0 ms) vs 25 ms budget → **perf PASS** |
| replay | ticks=15042 cmds=2155 hashes=150 **mm=0 → determinism PASS** |
| bots | atk reg=12 breach=360 crown=659 kills=88 deaths=12; def reg=0 (by design) |

## Root cause (drill-vs-tree interaction, NOT a sim bug)

`[gm-denied] tick=1494 name=t137a__00 verb=gm siege-start` — T-152's operator
allowlist (empty; `BH_GM_NAMES` unset) denied bot0's single-shot horn; battle
never started; `breach()` correctly refused while inactive. Pre-T-152 drills
passed on the ungated verb. Secondary: 7 fragmented bands (4+1+1+1+1+1+3).

## Fixes carded (T-157 law: file, don't fix inline)

- `T-157-F1-rehearsal-horn.md` (blocker): `BH_GM_NAMES` in drill + horn retry + abort-fast assert. Staff recommends (a)+(b).
- `T-157-F2-band-fragmentation.md` (quality): quintile muster.

## Re-verdict m4e29b (F1(a) implemented in drill, 2026-09-16)

`tools/t137_m4_leg.sh 7851 12 6 750 m4e29b`, `logs/t137_m4e29b.bwj` (force-added):

| Check | Result |
|---|---|
| battles | **1** (`battle joined until tick 1980000 (12 bands)` — horn fix works) |
| gates-down | **2/2** (Outer by t137a__03, Inner by t137a__05, ~tick 5400) |
| attuned | **0** |
| flips | **0 → M4 still FAIL** |
| p99 max | 2813 µs (2.8 ms) → PASS |
| replay | ticks=15601 cmds=2179 hashes=157 **mm=0 → PASS** |

Gap moved up the stack: 60 s uncontested attunement never opened vs 6 holders
(659 crown attempts). Same signature as the audit drill. Filed
`T-157-F3-heartstone-attune.md` (recommend (a) more attackers/longer clock
first). Driver note: script died post-soak before printing assertions
(`wait` on reaped pids under `set -e`) — guarded with `|| true`; verdict
recomputed from logs, all evidence lines quoted above.

## Landed this wave (docs)

- `docs/ops/friday-night-readiness.md` → epoch 29 / schema v14 / wire 242, `siege-start`, `/ban` current, M4 citation flagged.
- `README.md` gate-leg refs → epoch 29/t159 (T-158).
- CI replays `logs/t159.bwj` (T-154).

## Still owed (scheduled)

- F1 re-run → M4 re-verdict (appends here). 20×1800 s M1 soak, M2/M3 numbers.

## Re-verdict m4e30a (2026-09-16, F3 option a exhausted)

16v6 × 900 s @ epoch 30, `logs/t137_m4e30a.bwj` (replay mm=0, 186 hashes):

| battles | 1 (horn OK via F1 allowlist seat) |
| gates-down | 2/2 (Outer + Inner by t137a__00) |
| attuned | 0 |
| flips | **0 → M4 still FAIL** |
| p99 max | 8862 µs (8.9 ms) → PASS (< 25 ms) |
| bands | 16 regs → 7 enlisted (1+4+4+4+1+1+1, singletons persist) |
| crown attempts | 1119 vs 6 holders; 60 s window never opens |

Same contest-denial signature as m4e29b with numbers nearly tripled — (a)
does not move it. F3 now director-call between (b) sim-side revisit
(`kHeartCaptureTicks`/ring/grace + M4 re-proof) and (c) accept the standing
gate shape. Script died post-soak before assertions again (same `set -e`
reaping note as m4e29b); verdict recomputed from logs.

## Re-verdict m4e30c (2026-09-16, F2 living-quorum build NOT yet — baseline)

16v6 × 900 s @ epoch 30, `logs/t137_m4e30c.bwj`:

| battles | 1 |
| gates-down | 2/2 |
| attuned | 0 |
| flips | **0 → M4 still FAIL** |
| p99 max | 3.6 ms → PASS |
| bands | 16 regs → 6 enlisted (1+4+5+4+1+1; a full 5-stack formed) |
| replay | mm=0 |

7→6 bands (quintile reg-gate works when the quorum is real). Singletons
persist two ways: (1) early birds reg before mates welcome (want counted
welcomed, not static size); (2) dead quintile-mates hold the roster quorum
open for solo re-regs. Fix landed in bots (static size + living roster, both
muster and reg gates); m4e30d re-measures.

## Re-verdict m4e30d (2026-09-16, static+living quorum)

16v6 × 900 s @ epoch 30, `logs/t137_m4e30d.bwj` (replay mm=0):

| battles | 1 |
| gates-down | 2/2 |
| attuned | 0 |
| flips | **0 → M4 still FAIL** |
| p99 max | 3.6 ms → PASS |
| bands | 16 regs → 7 enlisted (1+4+4+5+4+1+1) |
| parties at end | 3×0, 8×4, 5×5 — 3 bots NEVER joined any party |
| replay | mm=0 |

Static+living quorum did not move it (7 bands again). Root cause refined:
the 60 s march release fires into a scattered column — 3/16 never complete
invites (refused past 12 tiles) — and dead-quorum re-regs mint fresh
singletons. Fix landed: the 60 s release fires only for incomplete/
diminished quintiles; full living quintiles hold to T+180. m4e30e measures.

## Re-verdict m4e30e (2026-09-16, march-hold refinement)

16v6 × 900 s @ epoch 30, `logs/t137_m4e30e.bwj` (replay mm=0):

| battles | 1 |
| gates-down | 2/2 |
| attuned | 0 |
| flips | **0 → M4 still FAIL** |
| p99 max | 3.3 ms → PASS |
| bands | 16 regs → 6 enlisted (1+4+5+4+1+1); parties 3×0/8×4/5×5 |
| replay | mm=0 |

Unmoved (7,6,7,6 across a–e: noise around 6–7). Structural insight, finally
the right one: **16 attackers make four natural groups (5+5+5+1) — bot 15
is always a singleton** (lead=(idx/5)*5). The F2 bar (≤4) was specced for
TWELVE (5+5+2 = 3 minimum). All 16-attacker legs test the wrong N; death
fragments add +2–3 on top. Card-literal 12v6 leg (`m4e30f`) measures the
real bar.

## Re-verdict m4e30f (2026-09-16, card-literal 12v6)

12v6 × 900 s @ epoch 30, `logs/t137_m4e30f.bwj` (replay mm=0):

| battles | 1 |
| gates-down | 2/2 |
| attuned | 0 |
| flips | **0 → M4 still FAIL** |
| p99 max | 3.0 ms → PASS |
| bands | 12 regs → 7 enlisted (5+1+2+1+1+2+1; re-regs inflate slots) |
| parties at end | 3×0, 4×2, 5×5 |
| replay | mm=0 |

A 5-stack forms every leg now (the doctrine works when co-located), but
pairs/singles never merge: no rendezvous mechanism exists post-march
(invites die past 12 tiles, nobody paths to each other, re-regs mint fresh
singletons). Five legs (7,7,6,7,7): bots-only levers exhausted. Re-file as
server-side band merge (re-reg joins the existing band; T-139 extended) —
beyond F2's bots-only scope.

## Re-verdict m4f2m (2026-09-17, F2 server-side merge LANDED → F2 CLOSED)

12v6 × 900 s @ epoch 30, `logs/t137_m4f2m.bwj` (force-add by director on
commit; server-log verdict — the post-R2 binary (31) refuses the epoch-30
journal by contract):

| battles | 1 (horn OK) |
| gates-down | 2/2 (Outer by t137a__03, Inner by t137a__00) |
| regs → bands | 12 regs → **4 enlisted bands (1+4+5+1, 11 members)** + 1 party-muster |
| F2 bar (≤4) | **PASS** (was 7 in m4e30f) |
| attuned | 0 |
| flips | **0 → M4 still FAIL** (attune gap = F3 director call (b)/(c), unchanged) |
| p99 | 1.39 ms → PASS |
| determinism | 3 unit pins + `m4e30f.bwj` re-replays mm=0 under the merge rule |

Devlog 0122 · card `done/T-157-F2.md`.
