# T-060 refine — DONE S16 (2026-09-05)

## Context

Soma anvil law, our cadence: the improvement verb that burns parts and gold
and occasionally the item itself. This is the gear-sink half of the churn —
durability wears it down (T-058), refine tempts it up (T-059 gilds it).

## Scope

- `InvSlot.refine` 0..3; weapon +2 dmg/tier, armor +1 def/tier (stacks under
  aura flat and affix multipliers — order: refine add, then whet 10%).
- Verb `/refine <slot>` at the Widow Anvil (proximity law via nearAnvil):
  consumes **1 monster part** (any junk slot) + **50g** per attempt.
- Mercy tiers: 0→1 and 1→2 guaranteed; 2→3 is a 60% coin — failure
  DESTROYS the piece ("the Widow keeps the iron"; log [anvil-refine]).
  Full durability required: mend before you anneal.
- Persist: inv blob field 7 (schema v10); wire ItemSlot + u8 refine; client
  row shows " +N"; ch-255 anvil line narrates the ring / shatter.

## Tests

- Proximity refusal, toll consumption (gold −50, junk consumed), guaranteed
  steps raise stats exactly +2/+4, durability<100 refusal.
- The coin: seeded strata walk (300 petitioners, one world) proves both
  outcomes — success to +3 and destruction — inside one binomial window.

## Evidence

- test_gear_churn.cpp T-060 case green (11 assertions, sawSuccess &&
  sawShatter pinned); suite 83/83 (327,466 assertions).
- Epoch-4 smoke replay 0 mismatches; journal epochs now 1..4 documented in
  main.cpp kJournalEpoch comment chain.
