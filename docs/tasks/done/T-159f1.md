# T-159f1 — Loot follow-ups (CLOSED 2026-09-17, ADR-0016 wave)

**Status:** `done` — all 5 items landed. Devlog 0123. Epoch 30→31, wire 244→245.
(F-WIRE correction 2026-09-18: first filed as 241→242 off the dead gen/
mirror; live builds run 245 via build-time regen — see ADR-0016.)

## Progress

- [x] 3. Client rarity chrome: `engine/render/rarity_chrome.h` (marker law:
  `M /R /U `, out-of-range renders common) + inventory row colour
  (Magic blue / Rare yellow / Unique ember over aura/equipped, under glow;
  dormant still wins) + bag-row marker prefix. Render-only: no wire (rarity
  already rides ItemSlot), no epoch, no schema. 2 pins
  (`tests/test_rarity_chrome.cpp`); suite green; linux-gcc client green.
- [x] 4. Boneyard deterministic pin: twin-world rate formulation (4000
  swings each, damage uplift; +250 floor ≈ several σ under the ≈+1200
  mean) — seed-proof by construction, appended to `test_affix_v2.cpp`.
- [x] 5. Drop-log instrumentation: `World::dropLog` + `setDropLogPath`
  (T-080 posture: default `logs/drops.log`, git-ignored, replay-duplicate
  harmless), one pinned line per gear/unique/junk/gold award in killMob.
  1 pin (`tests/test_droplog.cpp`). Enables the 200-kill all-tiers log
  (next: point a leg at it, no code).
- [x] 1. Multi-affix (ADR-0016: affix2/affix3 + 10-field blob, Magic 1–2 / Rare 2–3, epoch 31).
- [x] 2. Per-band tables: 45 rows, every matching row rolls (epoch 31).

## Scope

1. Multi-affix items (Magic 1–2, Rare 2–3): needs a schema change (affix is one u8) — own ADR + migration + blob grammar bump.
2. Per-band drop tables toward ~45 rows (wave-2 added +9 +11 mob rows; vendor/fence presence re-check).
3. Client rarity chrome (name colour + bag-row marker — wire already rides ItemSlot rarity since T-159).
4. Statistical Boneyard pin (seed-fragile — find a deterministic formulation).
5. Live 200-kill all-tiers drop log (drop-log instrumentation first).

Each item is its own commit; batch shares one epoch bump only if sim moves.
