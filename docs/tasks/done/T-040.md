# T-040 — Bot deaths counter underreports
**Fix**: CombatEvent kind=3 target==ownId; verified in the 3-zone gate (137 deaths observed).
OwnStats deltas coalesce over despawns: bots often never see hp==0 before the
respawn OwnStats. Use CombatEvent kind=3 self-target as death source instead.
