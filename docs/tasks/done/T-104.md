# T-104 — External-review hardening wave (verified 2026-09-17, code already in tree)

## Context

Critical review surfaced 6 bugs (inventory tombstone, trade duplicate, gold overflow, trade log dirs, respawn AoI ghost, parseInvBlob throw). Handover flagged card still open, but code has been in tree since `Done — T-104..T-110` (README) and is covered by `tests/test_t104_review_fixes.cpp`.

## Verification 2026-09-17 (branch `task/art-probe-and-b5-close` on `task/T-R-LUMA-rimlight:238/27`)

- `cmake --preset linux-gcc && cmake --build --preset linux-gcc -j4` warning-free
- `bh_tests 299/299` includes 9 `T-104*` cases (84 assertions) — `tradeOffer` dup fix, `vendorSellJunk` u64 math, `parseInvBlob` exception-safe, tombstone erase, spatial home_ insert. All 9 PASS.
- `ctest 2/2`, `validate 0/6`, `t146` mm=0 epoch 27 unchanged.
- Fixes present in `server/src/world.cpp` (`tryAnvil` erase, `tradeOffer` index loop, `vendorSellJunk` `std::uint64_t`, `create_directories` in `tradeCommit`, `respawnTick` `home_`, `parseInvBlob` bounded accumulator).

## Close-out

No code change — code was already shipped. Card moved `tasks/T-104.md` → `done/T-104.md` with this verification paste. No epoch/wire change.

## Deviations

- None — verification only.
