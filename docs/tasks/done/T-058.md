# T-058 durability — DONE S16 (2026-09-05)

## Context

Gear churn needs a fuse the player can read. Era oil: equipment wears from
use and a merchant spins it back for coin. The skeleton for the anvil's
"gear is never settled" thesis.

## Scope

- `InvSlot.durability` u8 (0..100) on weapons/armor only; persisted as inv
  blob field 5 (schema v8, column-free), wire `ItemSlot` + `u8 durability`.
- Burn: equipped weapon −1 per **landed** swing; equipped armor −1 per
  landed hit **received** while the defender is a player. No burn on whiffs.
- Dormancy at 0: slot kept (never destroyed), stat path mutes to
  `kFistsBaseDmg` (weapon) / 0 def (armor). Inventory row greys `[WORN]`.
- Repair: `/repair` verb → vendor-proximity (Marta turf, gallows-reds
  refused), all-or-nothing: missing points ÷2 in gold per piece, chat line
  with the toll; era shop-rate tax stands against the Widow's parts economy.

## Tests

- Weapon burn & dormancy pays fists; armor burn & dormancy strips DEF;
  live end-to-end burn signal (rat kill wears <100 and ≥90 durability);
  repair refuses without vendor / broke / chaotic; full suite unaffected.

## Evidence

- 7-case test_gear_churn.cpp (4 for T-058); suite 83/83 (327,466 assertions).
- S16 epoch-4 smoke: bots `mend=14` (live /repair use), replay OK
  `ticks=1455 hashes=14 mismatches=0` (logs/s16_smoke.bwj).
