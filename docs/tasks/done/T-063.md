# T-063 Bonehowl Mine + Drowned Crypt — DONE S18 (2026-09-05)

## Context

The world had three zones and one animating premise: what lives past the
bleak fields' east border, and what's under the crypt's vault. Both are
hunting-space, both priced by what they keep.

## Scope

- `tools/mapgen/make_bonehowl_mine.py` (mapId 4, 56×40): carved-from-rock
  drift with two galleries, ore spur, bone middens (WOOD props), damp vein.
  7 camp spawners (bats at the mouth, hounds ×3 on the drift, gnolls ×2
  galleries, widow vein NE), one portal pair (mouth ↔ fields east).
- `tools/mapgen/make_drowned_crypt.py` (mapId 5, 48×36): causeway over
  flood-water between chapel rows; 11 spawners; apse holds the boss.
- fields_overflow gains the east-mouth PATH + portal at (63,20);
  thornwall_crypt gains depths-stairs at (44,6); both ports reconverted
  + validated (`validate_links.py` — map table extended to ids 4/5).
- main.cpp zone-load tables (boot + replay mirror) extended to 4/5.

## Tests

- T-063 asset gate loads both zones through the real loader; pins sizes,
  portal targets exist, spawner counts (7 mine / 11 crypt).
- `validate_links`: 0 problems across 5 maps (walkable rects + arrivals +
  one map-id table sweep).

## Evidence

- tests/test_s18_dropzone.cpp T-063 case green; suite **91/91**
  (327,815 assertions); `logs/s18_smoke.*` replay OK (epoch 5, zones in hash).
