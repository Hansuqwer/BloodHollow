# T-069 — Fence at the Smugglers' Cove (S21)

## Context
GDD §7; phase3-remainder line: "Smugglers' Cove fence arrives with the
content drop … refusal now, fence later is the intended erasable gap."
Closes T-056's named gap. Marta's chaotic-refusal (T-056) stays.

## Scope
- New furniture kind `kWireKindFence = 69` (`shared/content/wirekind.h`;
  67/68 stay RESERVED per art REGISTRY: Bonesmith twins / confessor).
- Procedural seed: own spiral from `gallowsTile(1)`, skipping occupied
  furniture tiles (`World::spawnFence`; not a mapgen object — furniture is
  server-seeded per `spawnVendor`/bounty-board precedent).
- Semantics: fence pawns junk (slot 3) at **60%** for **any** karma band;
  secret 3-item stock (`kFenceStock` = 3002 Smuggled Vial + 4004 Widow Silk
  + 4005 Revenant Ash) sold at **25% markup**, chaotic eyes only
  (`karmaBandOf == 2`), fiction refusal line otherwise. Fence trade never
  moves karma.
- Lane routing in `applyWorldCommand` is **by item** (fence stock is
  item-disjoint from `kVendorStock`): `kBuy` → `fenceBuy` for fence stock,
  `kSellJunk` → `fenceSellJunk` when `nearFence(e)` (≤3, Chebyshev, mirroring
  vendor lanes). Deterministic → replay-exact. Marta's T-056 gates untouched.
- Client (judgment call, flagged): same vendor panel opens near the fence
  with Sable's crate appended (F6–F8, markup prices) via the existing
  `BuyRequest` path — no wire change, no new art; kind 69 renders through
  the generic furniture placeholder rect + name label.
- Incidental fixes bundled (same epoch, flagged): **one-Marta fix** (the
  vendor spiral had lost its break and seeded ~425 duplicate Martas —
  entity bloat, no free town tile; live soak entity count 544–584 → ~340
  confirms it) and **retaliation-on-miss + chase path-clear** (aggro fires
  on the strike itself; acquiring mob drops stale wander path).
- Epoch **7 → 8** (new furniture + entity set shifts the sim under old
  journals; same response as T-030/T-068).

## Acceptance criteria
- [x] Fence pays 60% to chaotic AND lawful; secret stock to chaotic only.
- [x] Marta still refuses chaotics both lanes (T-056 regression pinned).
- [x] Karma unchanged by any fence trade.
- [x] Exactly one Marta + one Sable seeded; Sable within gallows spiral.
- [x] Suite green, replay `mismatches=0` on fresh epoch-8 journal.
- [x] Client panel lists crate at the fence (F6–F8); G loots 60% there.

## Tests required
- `tests/test_fence.cpp` (7 cases): 60% pawn both bands, crate karma gate +
  markup, one-crate-only, `applyWorldCommand` lane routing, one-Marta,
  T-056 regression, town seed shape.
- Portal hash test hardened (eventual-churn loop — was coupled to the
  425-vendor entity layout; see devlog 0032 audit).

## Out of scope
- Karma repentance at the chapel (later card); fence buy of gear (no gear
  pawn lane exists — junk/slot-3 only, same as Marta); new art (placeholder
  rect); L8→L9 step-up (awaiting director).

## Evidence
- Suite: **112 tests / 327,969 assertions**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 600 s, epoch-8 journal `logs/t069.bwj`
  (entities ~330, p99 ~3.8–5.6 ms; campaign 0 deaths, killerByLvl empty).
- Replay: `[replay] OK ticks=14001 sessionCmds=5984 hashes=141
  mismatches=0 entities=343`.
- Devlog: `docs/devlog/0032-smugglers-fence.md`.
