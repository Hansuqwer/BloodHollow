# T-117 — Continuation prompt + session handover filed  [DONE 2026-09-12]

## Context

The T-116 merge wave left master at epoch 20 with an empty PR queue — the
review era is closed. The next build sessions need standing orders that match
the post-wave state exactly, not session memory. The continuation prompt was
therefore authored **from verified master state** (`c8d8087`): every number
in it was read out of the tree at authoring time (epoch 20, wire 237, schema
v10, suite 206/206 · 329,022 assertions, duel pin `b273be661b54673a`,
`t115.bwj` replay, entity kinds 1001–1014, kKit ids, mapIds 1–5, journal
grammar) rather than recalled.

## Scope

File, under the director's merge authorization
(`docs/prompts/merge-filing-2026-09-12.md`):

- `docs/prompts/continue-building-2026-09-12.md` — standing orders for build
  sessions: state snapshot, the law, card→PR workflow, verification battery,
  build queue (Track A: M3 party-crypt gate · Track B: Phase-4 spine ·
  Track C: debt), cheat sheet, traps, Arena-sandbox notes, startup
  checklist. Its next-card/next-devlog references were bumped to T-118/0080
  because this filing consumes T-117/0079.
- `docs/prompts/session-handoff-postwave-2026-09-12.md` — the session
  handover (ground state, what this session did, open director decisions,
  watch items, sandbox notes).
- `docs/prompts/merge-filing-2026-09-12.md` — the authorization record.
- This card + devlog 0079 + board row.

## Acceptance criteria

- All five docs on `master` via one squash-merged PR.
- No code, wire, schema, content, or epoch change (stays 20).
- CI green on both matrices; PR queue empty after.

## Tests required

None — docs-only. CI's full client build (ubuntu+macos) is the smoke gate.

## Out of scope

Any code change; any build-queue decision (Track A/B/C ordering stays
director-owned); consuming any further card or devlog numbers.

## Evidence

Docs-only PR, CI green (ubuntu+macos, full build + mapgen determinism +
ctest + mapconv validate), squash-merged under the filing authorization;
remote branch deleted post-merge; master CI green and queue empty after.
Deviations: none.
