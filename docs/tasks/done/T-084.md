# T-084 — L8→L9 step-up: widow north-edge camp (BOT-ONLY)

## Context
Oldest open decision (devlogs 0029/0030). `widow_glade` L9 (rect x[58,63] y[42,45]) shares the south bank with `gnolls_pits` L7 (x[50,57] y[42,45]); no L8+ waypoint existed — the L7 camp (53,41) was the ceiling probe. Director ordered the ladder push (1–10 run).

## Scope
- `tools/bots/main.cpp` only: if `b.level >= 8`, camp the widow NORTH edge (60,41). Widows (L9: dmg 22, aggro 6, wander 6, leash 10) pull onto the camp; gnolls (wander 8, aggro 7) still reach from the west as fodder via the defend branch. Retreat-home crosses the river north, same as the L7 camp. Pack cap + swarm panic-break unchanged.
- No mapgen change (both rects already on the clean south bank post-T-068; bridge route tiles unchanged). No mob numbers moved. No epoch bump (stays 12, bot-only).

## Acceptance criteria
- [x] L8 bots camp (60,41) and survive a 240 s val with zero L11 involvement.
- [x] Replay `mismatches=0` on the val journal.
- [x] Suite green, warning-free.

## Tests required
- Val leg (below) + replay. No unit tests (bot-profile waypoint, harness-validated per T-074 discipline).

## Out of scope
- Widow/gnoll stat tuning; map-regen; L9+ balance verdict (needs a full 540 s climb leg + chain evidence — follow-up if director wants the ceiling pushed to L9/L10).

## Evidence
- Val `logs/t084.bwj` (resumed `/tmp/t083b.db` → `/tmp/t084.db`, campaign pair sqlite-set L8/xp0/town (32,16), gear+gold kept, 240 s campaign-only `--target-level 9`, port 7897): `[replay] OK ticks=6001 sessionCmds=718 hashes=241 mismatches=0 entities=358`. Bots: kills 86, deaths **0**, killerByLvl empty, maxLevel 8 (held), shops 9, regear 3, mend 17. Zero L11, zero perch clustering.
- Suite: 160/160 (328,489 assertions), ctest 2/2, warning-free.
- Devlog: `docs/devlog/0050-l8-stepup.md`.
