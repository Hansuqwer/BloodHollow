# T-098 — Decal pressure probe: cap holds 2.4×, no chunking (READ-ONLY)

## Context
T-ART-09 chose a single 512-cap FIFO surface over per-chunk render targets "until profiling demands". This probe checks demand from 7 recent legs. No code change.

## Scope
- Desk analysis: per-leg kill totals (= upper bound on live blood decals — TTL 12000t covers the longest 640 s leg), live telegraph/circle callers, frame-cost class of the y-sort. No instrumentation (nothing to instrument — the bound is arithmetic).

## Findings

| leg | kills (all profiles) | bound on live blood | telegraphs live | circles live |
|---|---|---|---|---|
| t090 (climb) | 156 | ≤156 | 0 | 0 |
| t091 (grinder) | 189 | ≤189 | 0 | 0 |
| t094 (grinder) | 210 | ≤210 | 0 | 0 |
| t096a/b/c (pilgrim) | 77/77/89 | ≤89 | 0 | 0 |

- Peak **210 vs cap 512 (2.4× headroom), evictions 0 in every leg.** Telegraphs have no live callers (Gravemother sits on map 5; no bot profile goes there — T-091's rings currently bloom only in unit tests and future boss fights). Circles have no callers at all (Sanctuary/Wither unshipped).
- Frame cost: y-sort of ≤512 pointers + ≤512 ellipse draws per frame — noise beside the ground loop (3000 diamonds/frame on Thornwall). No profiling run needed; the bound dominates the measurement.
- The cap also self-defends: FIFO eviction drops the oldest blood first, which is exactly the decay order — overflow degrades gracefully into slightly-shorter blood, never into breakage.

## Acceptance criteria
- [x] Peak pressure bounded with numbers across 7 legs.
- [x] Verdict: cap holds, chunking card stays closed.

## Tests required
- None (read-only).

## Out of scope
- Per-chunk render targets (re-probe triggers below); telegraph/circle callers (their cards own them).

## Reopen rule
Live telegraph callers (map-5 traffic or boss-card soak with wind-ups firing), sustained kill rates 2× current peak, or a human session report of blood visibly popping out at the 10-minute mark.

## Evidence
- No code touched; suite 173/173 untouched.
- Devlog: `docs/devlog/0067-decal-probe.md`.
