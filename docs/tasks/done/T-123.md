# T-123 — Weeping Castle (Track B3): the siege stage stands (epoch 23)  [DONE 2026-09-12]

## Context

The queue's next card after T-122 (directive: continue executing the
pipeline, one card = one session = one open PR). B3 per
`docs/prompts/continue-building-2026-09-12.md`: the Weeping Castle **map**
(mapId 6) — mapgen + validators + portals; 2 destructible gates, courtyard
Heartstone, throne, defender spawn. The siege **law** (gate damage type,
crown channel, ownership flips, Saturday scheduler) is the B4 card; B3
ships the stage it will run on.

## Branching: stacked on #22

Master still sits at `818d661` with PR #22 (T-122) open, and the journal
epoch is a **globally-unique semantics key** — two open PRs both claiming
"epoch 22" would be journals that lie. So `task/T-123-weeping-castle` is
based on **`c0de563` (PR #22's head)**: the epoch chain stays linear
(21 registrar-era → 22 pledge-lite → 23 castle), and the diff of this PR
shrinks to exactly the castle work the moment #22 merges. **Merge order:
#22 first.** If the director merges this PR first, its squash carries
T-122's changes with it — stated here so that can never be a surprise.

## What shipped

- **Map** `tools/mapgen/make_weeping_castle.py` → `data/maps-src/
  weeping_castle.tmj` (56x44, deterministic LCG like every gen): moat with
  two causeways, curtain wall with **two 1-tile gate gaps** (west 20,34 /
  east 36,34), courtyard crossroads, walled keep nock with throne room.
- **Portals**: moor road ⇄ Bleak Fields East — the fields map gains an
  east-rim opening by the gnoll camp (`castle_road_in` 63,10; the rim
  stays shut between mouths). `validate_links.py` MAPIDS += 6; **0
  problems across 6 maps**.
- **Fixed pieces** (code-placed in `spawnCastle`, the confessor pattern;
  both live + replay spawn them): West/East **Gates** (wireKind 74,
  100,000 HP — GDD §8), **Heartstone** (75, courtyard crossroads, 150,000
  HP — B4-tunable stand-in, GDD prices neither it nor the toll), **Weeping
  Throne** (76, keep nook, furniture — the channel reads its occupant, not
  its HP).
- **The dead garrison**: new mob {1015, **Oathbroken Sentinel**, L14,
  Sexton base +2 levels, no boss kit, no guard flag — it aggroes anyone}.
  Four sentinel spawners (gate watches, heart ring, keep steps) + a Hollow
  Hound pack on the approach moor.
- **Boot**: zone 6 registered in both zone lists (live + replay mirror);
  `bh_maps` regenerates `weeping_castle.bhmap` every build (T-071 law —
  .bhmap stays gitignored, .tmj is the source of truth).
- **Client**: zero functional change — kinds 74–76 land in the generic
  furniture band (comment truth-up only). The castle tileset kit stays a
  GDD §11 placeholder-budget item.
- **CI (intended, blocked)**: the determinism gate should regenerate +
  diff the castle map too, and `validate_links.py` should run as its own
  step — but this session's GitHub App credential **cannot push workflow
  edits** (`workflows` permission). The two-step block ships verbatim in
  the PR description for a credentialed human to apply; the local
  battery ran both checks (castle diff clean, 0 problems across 6 maps).
- **Epoch 22 → 23**: a new zone's entities (garrison spawners + four
  pieces) shift every post-boot hash — the T-068 content-shift precedent,
  anticipated by the queue card itself.

## Evidence

- Suite **223/223 · 329,317 assertions** (+5 castle cases: portal graph,
  the four pieces on their mapgen tiles with GDD HP, garrison fill +
  content-table pin, walkable breach paths from the moor through both
  gates to the heart and throne, fields east-rim shape).
- ctest 2/2 — duel pin `b273be661b54673a` unchanged.
- Gate leg `tools/t123_castle_leg.sh` → `logs/t123.bwj` (epoch 23): five
  L25 fighters garrison the courtyard — **all five wave-2 logins are
  zone-6 l-lines**, combat churns (10 kills, 40 swings, 2 deaths — the
  walk of shame sends those home, era law), **4/5 hold the castle across
  the relog** (wave-3 l-lines zone 6; positions persist at map 6).
- Replay **`OK ticks=1022 sessionCmds=277 hashes=10 mismatches=0`**.
- Stacked-lineage guard: `t122.bwj` **refuses** on this build (journal 22
  vs build 23, exit 4) — correct posture; it remains the leg of record
  for the pledge lineage.

## Acceptance criteria

- [x] mapId 6 mapgen + `.bhmap` v2 conversion + validators (0 problems).
- [x] Portal pair wired (fields ⇄ castle), rim opening carved, graph closes.
- [x] 2 gates (100k HP) + courtyard Heartstone + throne + defender spawn,
      all code-placed deterministically.
- [x] Epoch bump + fresh leg of record (`logs/t123.bwj`, mm=0).
- [x] Suite + ctest green; CI extended (castle determinism + link graph).

## Tests required

Suite + ctest + the leg script above; CI on both matrices (PR).

## Out of scope

Siege law & scheduler (B4: damage types, crown channel, ownership,
`gm siege-now`); holdings & the steward NPC post (B5 — wireKind 73 stays
reserved); bot siege army (B6); anti-grief (B7); castle art beyond the
placeholder band.

## Deviations

- **Stacked on PR #22** (see above) — the first open-PR-predecessor in
  the pipeline's history; epoch linearity forced the choice. Merge #22
  first.
- Gates do not block movement yet: pathing is map-data law and entities
  never block tiles; the gap tiles are walkable PATH so B4's breach
  mechanics own the blocking question.
- Heartstone 150k HP is a flagged stand-in (B4-tunable).
- One leg bot died and respawned to zone 1 (4/5 residency) — reported as
  run, not retried away.
- **CI workflow edit dropped from the branch** (credential scope): apply
  the PR description's CI block with a `workflows`-permissioned account
  before or right after merge; nothing else depends on it.
