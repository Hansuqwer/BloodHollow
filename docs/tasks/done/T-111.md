# T-111 — Critical-review hotfixes (respawn spatial, anvil slot, inv-blob parse)

## Context
Post T-103 content + T-104..T-110 hardening wave. External/agent critical
review (`docs/prompts/critical-code-review-2026-09-12.md`) found residual
P0/P1 defects the prior cards did not cover.

## Scope
- **F1** `World::respawnTick`: cross-zone respawn inserts spatial at
  `home_` (gallows/bind), matching `walker.place` — was `spawnPoint`.
- **F2** `World::tryAnvil`: track equipped-weapon **slot index** through
  part-stack erases; never hold a raw `InvSlot*` across `inv.erase`.
- **F3** `parseInvBlob`: throw-free decimal parse (length + checked mul);
  oversized digit fields reject the record instead of `std::stoul` throw.
- **F4** `initialMobSpawns`: bounded scatter tries; no `uint32_t --n` wrap.
- **F5** `killPlayer`: brace/indent tidy on wanted-mark block (logic same).

No wire change. No journal-epoch bump (spatial is derived AoI state;
anvil index tracking preserves prior successful-rite outcomes; inv-blob
reject-on-overflow only changes hostile/corrupt inputs).

## Acceptance criteria
- [x] Cross-zone wanted/lawful respawn: `queryAoi` at body tile contains id.
- [x] Anvil tier-I with pelt stack *before* equipped shank still auras the shank.
- [x] `parseInvBlob` on 20-nines fields does not throw; well-formed tails parse.
- [x] Suite includes `tests/test_t111_review.cpp`.

## Tests required
- `tests/test_t111_review.cpp` — F1×2, F2, F3 (4 cases).

## Out of scope
- EntityStore SoA migration (T-110 debt).
- Filing retrospective cards for T-104..T-110 (docs follow-up).
- Fresh soak gate leg (no sim-hash field change expected).

## Evidence
- Prompt: `docs/prompts/critical-code-review-2026-09-12.md`
- Devlog: `docs/devlog/0073-critical-review-t111.md`
- Build: headless suite when toolchain available; CI matrix on PR.
