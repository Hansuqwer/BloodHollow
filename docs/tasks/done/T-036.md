# T-036 — Zones-in-process (World holds N zones) — DONE S8
World now owns `unordered_map<mapId, Zone>` (map+grid+spatial+spawners each),
Entity.zoneId, per-zone path/AoI/combat. `loadZone(3, crypt.bhmap)` auto-loaded
at boot (optional). Portal system: step onto portal rect -> transferToZone with
anti-ping-pong grace ticks + sandbox arrival fallback. Cross-zone combat/AoI
blocked at acquisition. Hash mixes zoneId. Unit tests: one-way + round-trip.
Live evidence: [zone] scout_00 -> map 3 at (24,30) + persisted map_id=3.
