# T-067 procedural audio pass 1 — DONE S19 (2026-09-05)

## Context

"Audio pass 1 is presence, not polish." Private-server era shipped
silence-then-OBS nostalgia; our stand: a hand-synthesized kit, zero
binary assets, fully reproducible from one header.

## Scope

- `client/src/synthkit.h`: raylib `Wave` 22050 Hz/16-bit/mono buffers
  baked once (`Game::bakeAudio()` after `InitAudioDevice`; skipped when
  BH_NO_AUDIO=1 or the device is absent — CI/headless safe).
- Voices: swing (0.10s falling sine swipe), hit (0.06s noise-thud body),
  BLOOD BOLT (0.22s downward chirp + breath), anvil toll (0.45s metallic
  partials), choir sting (0.35s minor-third onset, mend/bless), level
  chime (0.28s C5→G5), petrify-death (0.30s low rumble + dust).
  Total ≈ **78 KB** synthesized (well under the 200 KB kit ceiling).
- Trigger map rides the same callout kinds as T-066 (playCallout switch,
  floater-commit site). Misses and xp-shimmers stay deliberately quiet
  (ear-time budget).
- Deterministic LCG for all noise voices (audio is content, not entropy).

## Tests

- Headless suite unaffected (client-not-linked); BH_NO_AUDIO path
  smoke-verified by the s19 leg runner build shape.

## Evidence

- Suite **91/91**; S19 smoke replay 0 mismatches (audio client-side only).
- Client built clean with `InitAudioDevice` no `BH_NO_AUDIO` set; sounds
  play only after `IsAudioDeviceReady()` gate.
