# T-157-F2 — Siege-bot band fragmentation (CLOSED 2026-09-17, server-side merge)

**Status:** `done` — 12 regs → 4 bands (1+4+5+1) in `m4f2m`, bar ≤4 met.
Devlog 0122. No epoch (hash-neutral, T-139 precedent).

## Finding

12 attackers enlisted as SEVEN bands (4+1+1+1+1+1+3, server log) instead of ~3
quintiles. Quintile doctrine (devlog 0098) assumes mustering at spawn; invites
refuse past 12 tiles (T-138 law), so scattered arrivals band alone. M4's 8-band
cap was never stressed (7/8); a real 20v10 night could fragment worse and hit
the cap with singletons.

## Evidence update (2026-09-16): 7→6, two holes diagnosed

`m4e30a` enlisted 16 regs as **7 bands (1+4+4+4+1+1+1)**.
`m4e30c` (reg gated on quintile quorum): **6 bands (1+4+5+4+1+1)** — a full
5-stack formed, but singletons persist two ways: (1) `want` counted
*welcomed* mates, so early birds mustered/regged before the quintile
arrived; (2) dead roster mates held the quorum open for solo re-regs.
Fix landed (static quintile size + living roster, muster AND reg gates);
`m4e30d` re-measures against the ≤4 bar (pro-rated: 16 attackers ≈ 3–4).

## Evidence update 2 (2026-09-16): 7 bands again, race diagnosed

`m4e30d`: **7 bands (1+4+4+5+4+1+1)**; end-state parties 3×0/8×4/5×5 — 3 bots
never joined any party. The 60 s march release fires into a scattered
column (invites die past 12 tiles) and re-regs mint fresh singletons.
Refinement landed: 60 s release only for incomplete/diminished quintiles;
full living quintiles hold formation to T+180 (invites keep firing, cap
1000 s). `m4e30e` measures. If singletons survive that, the remaining lever
is server-side band merge (beyond bots-only — re-file, don't stretch).

## Scope

Tighten drill muster (wait-for-quintile before `/siege-reg`, or rally-point
hold until T+X). Bots-only; no sim.

## Acceptance

F1 re-run shows ≤4 attacker bands for 12 bots; doctrine note in the M4 devlog.

## Evidence update 3 (2026-09-16): card-literal 12v6 still 7 — re-file

`m4e30d`/`m4e30e` (16v6): 7 then 6 bands. Structural floor found mid-pass:
16 attackers make four natural groups (5+5+5+1) — bot 15 is always a
singleton — so `m4e30f` ran the card-literal 12v6: **7 bands
(5+1+2+1+1+2+1)**; parties 3×0/4×2/5×5. A 5-stack forms every leg (doctrine
works co-located) but pairs/singles never merge — no rendezvous mechanism
post-march, and re-regs mint fresh singletons. Bots-only levers exhausted
across five legs (7,7,6,7,7). **Re-file as server-side band merge (re-reg
joins the existing live band, T-139 extended). No more drill-only legs.**
