# T-096 — Multi-leg rite demo: banking works, lane proven, skill resets (DB DISCIPLINE)

## Context
T-086 showed single-leg grafting is out (30 pelts/skill 20/120g vs ~5 pelts/leg). This card runs the progression the fresh-DB harness cannot: two natural legs on ONE DB (pelts/gold/xp banked, same `--prefix` resume) + one bless-seeded acceptance leg proving the gate chain end-to-end.

## Scope
- Leg A: 3 pilgrims, 240 s, FRESH `/tmp/t096.db`, prefix `t96p_`.
- Leg B: same DB file, same prefix (characters resume with banked inv/gold/xp — no sqlite-set; positions evolve naturally, this is a progression demo, not a val).
- Leg C: FRESH `/tmp/t096c.db`, same prefix, `--bless` skill:20/gold:200/30 pelts/shank per pilgrim (smoke-test seeding lane, journaled b-lines, replay-safe) — acceptance proof, not economy cheating.
- No code change. No epoch bump (stays 14).

## Acceptance criteria
- [x] Banking measured per leg from DB blobs (not just bot logs).
- [x] Lane proven or binding gate named.
- [x] Replays `mismatches=0` on all three journals.

## Tests required
- Three soak journals + replays. No unit tests (harness demo).

## Out of scope
- Toll changes (T-093 owns the decision); skill persistence redesign (finding below).

## Evidence
- Leg A `logs/t096a.bwj`: OK 6001/995/241 mm=0. 77 kills / 22 deaths, anvilTries 0. Banked: 8 pelts, gold 150–267, L3. Blades already near-dormant (dura 4/0/0).
- Leg B `logs/t096b.bwj`: OK 6001/993/241 mm=0. 77 kills / 50 deaths (hot resume), anvilTries 0. Banked: **18 pelts** (+10), gold 343–542, L4 reached. Same names resumed (t96p__00/01/02) — the discipline works.
- Leg C `logs/t096c.bwj`: OK 6001/988/241 mm=0. **anvilTries=3, grafts 3/3** (`[aura] tier=1 res=ok mercy` ×3). THE LANE WORKS — server accepts when all gates hold.
- Findings:
  1. Pelts/gold/xp bank across legs (drops survive death for neutrals) — ~9 pelts/leg pace → 30 pelts ≈ 4 legs.
  2. Sword skill is NOT persisted (schema `saveProgress` has no skill column; login seeds from bless or 0) — it resets every login. Multi-leg skill banking is impossible by schema; long single sessions are the only natural path to 20. Design finding for T-093/B.
  3. Observation (not a claim): post-leg-C blades read aura 0/dura 0 despite 3 accepted grafts in a 42-death leg — likely blade turnover (spare shanks appear in inv), needs a controlled single-death probe to close. Does not touch the acceptance proof ([aura] lines + replays).
- Suite: 173/173 untouched.
- Devlog: `docs/devlog/0065-rite-demo.md`.
