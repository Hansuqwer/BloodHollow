# T-126 — PR #26 merge wave: T-124 + T-125 land on master  [DONE 2026-09-14]

**Owner:** arena session `arena/01a09c1b-bloodhollow`
**Type:** merge wave / record-keeping (no product code)
**Authorization:** `docs/prompts/merge-pr26-2026-09-13.md` — director's
instruction *"Make a prompt to merge. Execute prompt."* This session only,
**not precedent**. It reverses this session's earlier "leave PR #26 open"
instruction and supersedes the "never self-merge" line in the merged
handover, for PR #26 only.

## What merged

**PR #26** → master, squash, **PR title as the subject**:

```
2845a3d T-124 + T-125: PR #25 merge wave, then the M3 gate follow-on r18-r22 (font reach 4/5 at 232 s; boss verdict still open)
2a813ec T-118: M3 gate — r8→r17 raider series (font reached once; boss verdict not established) (#25)
```

- PR state **MERGED**, `mergedAt=2026-09-14T06:26:33Z`, squash commit
  **`2845a3dda3eccad438c6fc7287a1ec32da711824`**.
- Source branch head at merge time: **`02dfa1d`**. The branch was **not
  deleted** — it is this session's pinned branch and stays alive. Deliberate
  departure from the four earlier waves.
- Contents: T-124 (PR #25's merge-wave evidence) + T-125 (M3 gate follow-on
  r18→r22: the Firebolt kit-channel law, distance-aligned marks, the staged
  map-5 route), six leg journals, and the merge prompt itself.

## Preconditions verified before merging

| check | result |
|---|---|
| master unmoved | `2a813ec` ✓ |
| PR #26 | `MERGEABLE` / `CLEAN`, head `02dfa1d` = local HEAD ✓ |
| CI on the merged head | `build-test (ubuntu-latest)` + `(macos-latest)` **success**, two runs (34813243843, 34813247672) ✓ |
| `bh_tests` | **209/209 · 329,058 · 0 failed** ✓ |
| `bh_duel --selftest` | `win=1 ticks=41 hpEnd=82 hash=b273be661b54673a` ✓ |
| replays (r17 journal + 6 legs) | all **mm=0** ✓ |
| r17 journal md5 | `b4c0cf3f3255e38071c172cc3a3336bd`, byte-identical ✓ |
| diff scope | **no `server/`, `shared/`, or `client/` changes** — bots, docs, journals ✓ |

Replay lines from the merged head:

```
m3_gate      ticks=36042 sessionCmds=5933 hashes=360 mismatches=0 entities=189
m3_gate_r18  ticks=18042 sessionCmds=4736 hashes=180 mismatches=0 entities=188
m3_gate_r19  ticks=17992 sessionCmds=4752 hashes=179 mismatches=0 entities=186
m3_gate_r20  ticks=18042 sessionCmds=4789 hashes=180 mismatches=0 entities=186
m3_gate_r21  ticks=18042 sessionCmds=4400 hashes=180 mismatches=0 entities=186
m3_gate_r22  ticks=18042 sessionCmds=4742 hashes=180 mismatches=0 entities=188
m3_gate_r22b ticks=18042 sessionCmds=5081 hashes=180 mismatches=0 entities=186
```

## Post-merge verification

- **Tree identity:** `git diff origin/master 02dfa1d` is **empty** —
  `IDENTICAL TREES`. A squash of a branch whose parent is master must leave
  the trees equal; this is the check that would catch a bad renumber.
- Master CI: see the run on `2845a3d`.
- Ground truth unchanged: epoch **21**, schema **v11**, wire **237**, duel
  pin **`b273be661b54673a`**, suite **209/209 · 329,058**.

## Incidents handled during the wave

1. **`build-test (macos-latest)` failed at the cmake Configure step** on
   `71ab8d5`, with Build and Tests skipped. Diagnosed as transient, not
   assumed: the identical source tree had passed macos at `7d47729` ten
   minutes earlier and the only intervening change was one new markdown
   file. The failure log was **unreadable** — GitHub's results-receiver
   returned `EOF` on six fetch attempts plus a zip download — so the
   diagnosis rests on the re-run, not on a log. `gh run rerun --failed`
   refused ("cannot be rerun") and the API rerun returned
   **`403 Resource not accessible by integration`** (no `actions:write`), so
   an **empty commit** (`02dfa1d`) was pushed to force a fresh run. Both
   jobs then passed, twice. **Merge was held until that was true** — the
   poll loop's first pass had a bug that printed `CI_GREEN` on a `fail`
   row, which is exactly why the check was re-read before merging.
2. **`.git` rolled back a second time** (to `818d661`, pre-PR-#25),
   destroying local commit `39dbe3d` while the working tree survived
   intact. Recovered with `git fetch` + `git reset --mixed 4e2c298` and a
   re-commit as `7d47729`. No force-push; nothing on the remote was
   rewritten. This branch has now lost committed work to a rollback twice —
   **push each commit as soon as it lands.**

## Queue state after this merge

- **#22** (T-122 pledge-lite, `c0de5634`) and **#23** (T-123 Weeping Castle,
  `e0718d7d`, stacked on #22) both edit `tools/bots/main.cpp` and both
  **conflict against master** (verified `git merge-tree --write-tree`); both
  carry `docs/devlog/0084-*` / `0085-*`, which master has taken. They stay
  open; their owner rebases and renumbers.
- `arena/01a09c1b-bloodhollow` remains open as a branch with its wave
  evidence on top of master.

## Counters after this wave

Next card **T-127** · next devlog **0088** · master **`2845a3d`** · epoch
**21** · schema **v11** · wire **237** · suite **209/209 · 329,058**.

## What this merge does NOT claim

**The boss verdict is still NOT established.** Six legs reached the depths,
`bossSeen` climbed 12 → 26, and there is not one Gravemother kill in the
record. What landed is that the reach stopped being luck (r19/r20/r21: 4/5 at
232–238 s) and that the crossing got a staged route. r22's kiter band was
**inconclusive, not measured red** — neither of its two legs reached the code
— and it was reverted for that reason. No TTK is claimed in either
direction.
