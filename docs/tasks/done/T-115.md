# T-115 — Lineage reconciliation: T-111+T-112 over the T-104 wave (epoch 20)  [DONE 2026-09-12]

## Context

Two parallel review lineages both declared `kJournalEpoch = 19` with
different content: PR #9 (`task/T-104-review-fixes`, six fixes + vendored
sqlite) and PR #8 + #11 (`arena/01a09574-bloodhollow` →
`task/T-112-t111-epoch-followup`, five T-111 fixes + T-112's epoch
follow-up). `git merge-tree` showed content conflicts in
`server/src/world.cpp` + `tests/CMakeLists.txt`; whichever lineage merged
second would need conflict resolution, a re-bump to 20 and a fresh gate
leg. This card does that work in advance, verified, so the director's
merge sequence is mechanical.

## Scope

Merge `task/T-112-t111-epoch-followup` (9e5c7d4) into
`task/T-104-review-fixes` (19b595f), resolving:

- **`server/src/world.cpp`**
  - `parseInvBlob` — duplicate fix (T-104 #6 ≈ T-111 F3): kept the T-104
    u64-overflow accumulator; both lineages' tests pass under it (verified:
    20-nines / 30-digit / 12-digit-qty rejects, in-domain accepts).
  - `respawnTick` — duplicate fix (T-104 #5 ≈ T-111 F1, byte-identical
    one-liner `spatial.insert(e.id, home_.x, home_.y)`): kept T-104's
    wording, noted the independent discovery.
  - `tryAnvil` — **union**: T-104's destroy-erase (#1) rewritten onto
    T-111 F2's `wslotIdx` index tracking (`e.inv.erase(begin + wslotIdx)`;
    no pointer scan, no `itemId=0` tombstone, index honest through the
    toll-loop erases).
  - F4 bounded scatter, F5 killPlayer tidy, tradeOffer index loop, gold
    u64 widening, trade-log dir creation: auto-merged, untouched.
- **`server/src/main.cpp`** — `kJournalEpoch` 19 → 20 (the double-19
  collision resolved; v19 legs of BOTH lineages are stale by contract).
- **`tests/CMakeLists.txt`** — all three review TUs
  (`test_t104_review_fixes` + `test_t111_review` + `test_t112_spawn_epoch`).
- Fresh gate leg `logs/t115.bwj` (20 campaign bots × 10 s) at epoch 20.
  #11's `t112.bwj` (v19) retained as history; `t115.bwj` is the leg of
  record for the reconciled tree.

## Acceptance criteria

- [x] Suite 206/206 headless (194 + 5 + 4 + 3), 329,022 assertions; ctest 2/2.
- [x] `bh_duel --selftest` pin unchanged: win=1 ticks=41 hpEnd=82
      hash=b273be661b54673a.
- [x] Epoch guard refuses both stale lineages' v19 legs (t104, t112) with
      the contract message.
- [x] `t115.bwj` replays clean: `ticks=202 sessionCmds=302 hashes=3
      mismatches=0`; soak p99 = 1.27 ms.
- [x] Every fix from both lineages present in the merged tree (fix
      inventory in the PR description).

## Tests required

None new — the merged suite IS the test surface (all three review waves'
regression cases run against the reconciled code). The two duplicate-fix
dedups are covered by both parents' tests, which now run together.

## Out of scope

- The merge order itself (director's call; playbook in the PR description).
- PR #10 (B5 art) — orthogonal, zero file overlap with either lineage.
- Master-side docs PRs #12/#13 — no code interaction (known trivial
  board-junction doc overlap, same as every done-section).

## Deviations

None — pure reconciliation, no behavior chosen beyond the two documented
dedups (each keeps one of two equivalent implementations).
