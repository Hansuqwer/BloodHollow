# T-073 — Gate guards + spawn-camp protection (S24)

## Context
M3 exit (guard-murder) + M4 anti-grief list + Alpha hardening.

## Scope
- **Guard mob 1011** (L15, hp 400, dmg 30, def 18, xp 0, aggro 0, wander 0,
  leash 12, `guard` flag): wanted-only acquire inside leash. Two spawners
  (east gate + bridge approach).
- **Wanted:** unlawful PK ≤8 tiles of a guard anchor → 240 s. Guards aggro,
  vendors refuse, death binds at gallows.
- **Spawn protection:** 100 ticks on spawn + respawn; lookup skips.
- **Epoch 10 → 11** + fresh leg replay.

## Acceptance criteria
- [x] Wanted set/expire ticks; guard aggro only on wanted.
- [x] Spawn protect suppresses aggro for exactly 100 ticks.
- [x] Gallows interplay; vendor refusal while wanted.
- [x] Suite green; replay `mismatches=0` on fresh epoch-11 journal.

## Tests required
- `tests/test_guards.cpp` (8 cases): row pins, wanted set/far-murder,
  guard acquire/ignore, expiry stand-down, vendor refusal + restore,
  gallows bind, 100-tick protection.

## Out of scope
- Guard-murder consequences beyond refill; karma repentance; L8→L9.

## Evidence
- Suite: **134 tests / 328,092 assertions**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 540 s, epoch-11 journal `logs/t073.bwj`
  (campaign 0 deaths, killerByLvl empty; death totals flat vs prior legs).
- Replay: `[replay] OK ticks=12801 sessionCmds=7321 hashes=129
  mismatches=0 entities=361`.
- Devlog: `docs/devlog/0036-gate-guards.md`.
