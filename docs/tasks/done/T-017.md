# T-017 — Chat (local/global/EK) + reconnect handling (S3 netcore)  [DONE 2026-09-04]

Chat channels 0=say (AoI), 1=global, 2=system; Enter focus-capture UI, "."-prefix = say, 0.5 s rate limit, sanitize printable <=160, 12-line log + composer; refresh/spawn diff handles reconnect-view; client system line on welcome verified; bots chatter 3%/tick decision exercised end-to-end (pkts counts in soak logs).

**Tests required:** per AGENTS.md. **Evidence:** verified on this branch, see
docs/devlog/0003-sprint-3-4.md.
