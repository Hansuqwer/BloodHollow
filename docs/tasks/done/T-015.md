# T-015 — Zone server: 20Hz tick loop + entity store + spatial hash + Walker intents (S3 netcore)  [DONE 2026-09-04]

server/src/main.cpp + world.* — 20 Hz fixed tick with <=2 ms enet_host_service budget inside the tick, resync guard; deque Entity store + SpatialGrid<8> (shared/sim/spatial.h, test_spatial.cpp brute-force churn + AoI window + no-dup checks); per-session command fair-share (1 cmd/session/tick); [soak] p50/p99/worldHash line every 30 s; --soak-secs/--p99-budget-ms self-grading exit 0/2.

**Tests required:** per AGENTS.md. **Evidence:** verified on this branch, see
docs/devlog/0003-sprint-3-4.md.
