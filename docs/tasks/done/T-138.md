# T-138 — Pledge-lite rebase onto Phase-S stack (Phase P, 1/3)

## Context

PR #22 (`c0de563`, T-122 pledge-lite, Track B2) predates the whole Phase-S
stack (#29→#41, epochs 21→25, schema v11→v12). This card rebases it on top
of `task/T-137-defenders-m4` via `git cherry-pick -n c0de563`: pledge core
(create/emblem/ranks/chat) + registrar + `pledge` bot profile + T-122 leg
assets, resolved against siege rehearsal/bots/defenders.

## Scope

- Journal kinds 34–40 (`kPledge*`, post-T-133 append), `g`-sidecar login
  line, registrar entity (wireKind 72, town square ≤3 tiles).
- Schema v12→**v13** (pledge_id/pledge_rank columns + pledges registry);
  `saveProgress` +2 params (16→18 args).
- Epoch 25→**26** (registrar is world composition, T-112 spawn law).
- `tools/bots/main.cpp`: union usage line; siege AND pledge profile blocks
  kept; fighter gate siege-aware (`|| raider || (siege && !siegeDefend &&
  stage 1-2)`); wander whitelist gains `&& profile != "pledge"`.
- Board: T-122 row appended with rebase note (T-121 precedent).
- OUT: pledge bands in siege bands + vault spending (follow-up cards).

## Acceptance criteria

- Full battery green; fresh `logs/t138.bwj` leg replays mm=0 with epoch 26;
  guard refuses `t135.bwj` (25 vs 26) exit 4; duel pin unchanged.

## Tests required

- Adopted `tests/test_pledges.cpp` (T-122 suite); `test_weapon_skill_persist`
  updated to 18-arg `saveProgress`.

## Deviations

- `persist.cpp` loadSiege/loadPledges splice repaired (cherry-pick
  multi-pass resolution had spliced loadPledges inside loadSiege's row
  block — restored closing brace/finalize/return).
- T-122 leg assets renamed t122→t138 on re-run (fresh epoch-26 leg of
  record is `logs/t138.bwj`).
