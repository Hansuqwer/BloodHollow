# T-121 — PR #19 merge wave: weapon-skill persistence lands as T-120 (epoch 21)  [DONE 2026-09-12]

## Context

The director said **"Execute."** — accepting this session's offer to run the
next merge wave on PR #19, another session's completed, CI-green execution
of Track B1 (weapon-skill persistence). PR #19 carried the second
cross-session counter collision: authored as **T-118** from `c8d8087`,
while master had since consumed T-118 (M3 gate, PR #18) and T-119 (wave
evidence, PR #20). Unlike the first wave, this target **changes server sim
semantics** — journal epoch 20→21, persist schema v10→v11, journal l-line
v2→v3 — so the full local battery on the synced branch was mandatory.

## Scope (executed under `docs/prompts/merge-pr19-2026-09-12.md`)

- **Pre-wave record**: authorization prompt committed and pushed to
  `task/T-121-pr19-merge-wave` before any merge.
- **PR #19 sync + renumber** (`ddad769`): card `done/T-118.md` →
  `done/T-120.md`; devlog `0080-t118-weapon-skill-persist.md` →
  `0082-t120-weapon-skill-persist.md`; **binary leg collision** — their
  epoch-21 `logs/t118.bwj` vs master's M3 `logs/t118.bwj` resolved by
  keeping master's byte-identical and writing theirs to `logs/t120.bwj`;
  script → `tools/t120_weapon_skill_leg.sh` (repo-relative paths — the
  only functional deviation); code comments renumbered; board junction
  resolved — a pre-renumber copy of the M3 row their branch carried was
  dropped (master owns the canonical T-118 row) and their weapon-skill row
  moved from the file bottom to the junction in card order.
- **Full battery on the synced branch**: offline build, suite **209/209 ·
  329,052 assertions**, ctest 2/2, duel pin `b273be661b54673a` (combat
  math unchanged), `t120.bwj` replay `ticks=1500 sessionCmds=529 hashes=15
  mismatches=0`, epoch guard refuses `t115.bwj`/`t118.bwj` (journal 20 vs
  build 21) by contract. CI green on the sync head, both matrices.
- **Merge**: squash-merged as **T-120** (`7b2e9b8`); remote branch
  deleted (no children); PR retitled, renumber documented in a comment.
- **This filing**: card + devlog 0083 + board row + post-T-120 amendments
  to both standing prompts (continuation prompt: journal l-line v3 grammar
  + pin amendment; pipeline prompt: battery pins amendment). Docs never
  drift — the prompts' §2/§5 numbers were pre-epoch-21 and are now
  corrected by dated amendment rather than silent rewrite.

## Outcome

Master `7b2e9b8` at journal **epoch 21**, schema **v11**: sword skill and
swing lands persist across relog (the T-096 "Sisyphus ladder" gap is
closed — the aura grind is real progression now), the journal l-line
carries skill (v3) with v1/v2 replay backward compat, and `logs/t120.bwj`
is the leg of record. Track B1 is DONE; next card **T-122** (Track B2
pledge-lite) per the queue.

## Acceptance criteria

- [x] PR #19 merged with the collision resolved by renumber; master's
      numbering preserved.
- [x] Binary leg collision resolved without touching either journal's
      bytes; renamed leg replay re-verified mm=0.
- [x] Full battery re-run on the synced branch (sim-semantics change).
- [x] Standing prompts amended for epoch 21 / schema v11 / l-line v3.
- [x] Master CI green; queue reported honestly (parallel sessions' new
      PRs stay open, out of wave scope).

## Tests required

The synced branch's full battery + CI on both matrices were the gates
(3 new tests from T-120 included in the 209).

## Out of scope

Track B2+ work; renegotiating the v3 journal grammar; any further card
numbers beyond T-121.

## Deviations

Their script's build-dir paths were repo-relativized (documented in the PR
comment); their branch's board edit carried a stale pre-renumber copy of
the M3 row, dropped in the junction resolution (master is canonical).
