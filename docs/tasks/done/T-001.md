# T-001 — Repo + CMake + presets + CI (S1)  [DONE 2026-09-04]

Scope: monorepo skeleton, CMake (C++20, -Wall -Wextra -Werror via interface lib),
presets (linux-gcc/clang, macos-arm64/x86_64, headless), vendored doctest +
nlohmann/json (SYSTEM includes), raylib 5.5 via FetchContent pinned by SHA-256.

Acceptance evidence:
- `cmake -S . -B build -G Ninja && cmake --build build` green (Debian, gcc 14.2).
- ci.yml matrix ubuntu-latest + macos-latest with X11/GL dep install step.
