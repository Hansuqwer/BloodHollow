# T-116 — Merge wave: the post-T-103 review era lands on master  [DONE 2026-09-12]

## Context

Seven open PRs spanning two review lineages, B5 art, and two logistics
tasks, with a known epoch-19 collision between them. The director
authorized a single scripted sequence — "Make a prompt and merge. Execute
prompt." — scoped to this wave only (prompt:
`docs/prompts/merge-wave-2026-09-12.md`; no other session may cite it as
precedent for self-merging). T-115 pre-resolved the lineage collision so
the wave would be mechanical.

## Execution (order as run)

1. **PR #12** (T-113 design) — squash-merged. Branch deleted; the raw
   delete auto-closed child **PR #13** (raw `git push --delete <base>`
   closes child PRs without retargeting) — replacement **PR #15** opened
   from the same head (80053c6) with the substitution documented.
2. **PR #9** (T-104 wave, six fixes + vendored sqlite) — squash-merged;
   branch retained until children were retargeted, then deleted.
3. **PR #15** (T-114 bots v3) — board junction conflicted after #9
   (same-spot section inserts); synced by merging master in and taking
   the branch side (it already contained master's identical T-113
   section plus T-114's). Squash-merged; branch deleted.
4. **PR #10** (T-ART-B5 procedural NPC sheets) — squash-merged; the
   three-way deduped the T-104 content already on master via #9 (final
   tree verified: vendored sqlite + epoch 19 + B5 assets all present).
   Branch deleted.
5. **PR #14** (T-115 reconciliation) — retargeted to master via REST
   PATCH, synced (board junction interleaved: T-113 + T-114 sections
   slotted between T-112 and T-115; code conflicts resolved to the
   reconciliation union — epoch 20, union tryAnvil, three review-lineage
   test TUs), then merged as a **true merge commit** (0355367): the
   reconciliation graph is the point. Branch deleted.
6. **PR #8** — auto-closed MERGED by GitHub one second after #14: its
   head (5634033) became reachable from master through the
   reconciliation ancestry; semantically the intended supersession,
   arrived via graph reachability. No extra commit landed.
7. **PR #11** — closed as superseded with a final comment (its content
   is on master via #14; it stayed open only because its base was a
   session branch, not master).

## Post-merge verification on master (0355367) — all pass

- Offline headless build: pre-seeded ENet deps + vendored sqlite
  amalgamation, zero downloads; server + tools + tests built clean
  (RelWithDebInfo, `BH_BUILD_CLIENT=OFF`).
- Suite: **206/206 cases, 329,022 assertions, 0 failures**; ctest 2/2.
- `bh_duel --selftest`: win=1 ticks=41 hpEnd=82 hash=`b273be661b54673a`
  (deterministic pin unchanged).
- `bh_server --replay-world logs/t115.bwj` → `ticks=202 sessionCmds=302
  hashes=3 mismatches=0 entities=191` (epoch-20 leg of record).
- Epoch guard refusals by contract: t107.bwj (journal 18 vs build 20),
  t104.bwj (19 vs 20), t112.bwj (19 vs 20).
- Master CI green on all four merge commits (ubuntu + macos: full
  client build, mapgen determinism, ctest, mapconv validate).

## End state

Master at `kJournalEpoch = 20` carrying the union of both review
lineages + B5 art + the T-113/T-114 logistics verdict, with
`logs/t115.bwj` as the replaying gate leg of record. PR queue empty.
Devlog 0078 · prompt `docs/prompts/merge-wave-2026-09-12.md`.
