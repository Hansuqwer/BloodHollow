# T-089 — Karma/price tuning validation: pins hold, questions framed (NO CHANGE)

## Context
Open director items: repentance amount/cooldown (+20/72000t, T-075 derivations), lantern price (150g, T-071 overnight pin), human trade-pass UX (T-033 human slice). Tuning numbers without director pins would violate the no-renegotiation rule — this card validates the derivations against 8 new legs and frames the decisions.

## Scope
- Verify each pin's derivation still holds on current tree; check soak legs for contradictory evidence; run the pinning unit tests. No value changed. No epoch bump (stays 12).

## Findings

| pin | derivation | 8-leg evidence | verdict |
|---|---|---|---|
| Repent +20 karma | one whitening-hour at pinned +1/3600t (`world.cpp` whitening block) | bots never repent (no `/repent` in bot profile) — no contradiction possible headless | HOLDS (test_repent pins green, 413 assertions across the sensing suite) |
| Repent 72000t CD | one logged hour; separates chapel grace from gate law (wanted refused) | zero repent attempts in any journal; wanted lane untouched in all legs | HOLDS |
| Lantern 150g | 18.75 torches = ~94 min of burn vs infinite +2 radius; keeps torch an 8g sink (T-076/B depends on it) | bots never buy 3004 (profile buys shank/blade/armor/vials only); torch economy healthy (vials/shops flow every leg) | HOLDS |
| Torch 8g / 6000t | unchanged since T-071 | lights used in legs (lanternLit/torch branches fire; no surprisals in server logs) | HOLDS |
| Trade-pass UX | human-only slice (T-033) | programmable half green (suite trade/commit tests pass; vendor/fence lanes flow in every soak) | UNBLOCKED for human review; nothing programmable left |

## Acceptance criteria
- [x] Every pin re-derived and checked against fresh legs.
- [x] Zero values changed; precise director questions framed (below).
- [x] Suite green (167/167).

## Tests required
- Pinning suites re-run (22 cases / 413 assertions green) + full suite 167/167.

## Out of scope
- Changing any number (needs director pins); human trade-pass session (needs a human).

## Director questions (defaults = keep; answer to change)
1. Repent +20/1h: keep as one-whitening-hour, or scale with level/deficit?
2. Lantern 150g: keep (torch-sink intact), or move to pull lanterns into bot-accessible economy?
3. Trade-pass UX: schedule the human slice — programmable half is green and waiting.

## Evidence
- Suite: **167/167 (328,566 assertions)**, ctest 2/2.
- Devlog: `docs/devlog/0058-tuning-validation.md`.
