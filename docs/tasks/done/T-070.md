# T-070 — Blood Curse + chapel cure (S22a)

## Context
GDD §9; T-061/062 shipped night buffs/economy but the debuff loop is "hook
tag only". Day/night completion, first of two cards (with T-071).

## Scope
- **Curse:** `Entity.curseUntil` (ticks). Boss Blood Bolt hits (T-064 cast
  path — Gravemother today) apply curse for **30 s (600 ticks)**.
- **Effect:** potions and Mend heal **75%** while cursed; OOC regen
  untouched. Bless NOT scaled (no heal component — flagged).
- **Confessor:** `kWireKindConfessor = 68` in the chapel rect (9,9)–(14,13);
  `/confess` (journaled `kConfess`) clears within 3 tiles + fiction line.
  Karma repentance out of scope.
- **Client:** violet CURSE buff row from `OwnStats.curseTicksLeft` (new
  trailing u16; version stays 237 by count law — S16 precedent, stated in
  devlog); confessor via generic furniture rect + label.
- **Epoch 8 → 9** + fresh leg replay.

## Acceptance criteria
- [x] Bolt hit sets `curseUntil = tick + 600`; potion/Mend ×0.75 while cursed.
- [x] Exact expiry at 600-tick edge; OOC regen untouched.
- [x] Confessor clears ≤ 3, karma unmoved, quiet fail otherwise.
- [x] Client violet CURSE countdown; protocol field documented.
- [x] Suite green; replay `mismatches=0` on fresh epoch-9 journal.

## Tests required
- `tests/test_curse.cpp` (7 cases): bolt application + 600-pin, potion 75%,
  Mend 75%, edge expiry, confess gates, `applyWorldCommand` round-trip,
  town seed shape.

## Out of scope
- Karma repentance; Bless scaling; Gravecaller bolt (no such kit — flagged);
  new art; new vfx kind.

## Evidence
- Suite: **119 tests / 328,008 assertions**, ctest 2/2, warning-free.
- Soak: 14-bot grinder mix, 580 s, epoch-9 journal `logs/t070.bwj`
  (campaign 0 deaths, killerByLvl empty; mend=11 live).
- Replay: `[replay] OK ticks=13601 sessionCmds=6093 hashes=137
  mismatches=0 entities=342`.
- Devlog: `docs/devlog/0033-blood-curse.md`.
