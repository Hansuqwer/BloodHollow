# T-161b — Kit spine part 2 (filed 2026-09-16, after T-161 Resurrect)

**Status:** `done` — closed 2026-09-16 on the M3c verdict below. All six
items shipped + hotkeys + M3 re-measured. Epoch stayed 30 throughout (every
item epoch-neutral by construction, proven by seven clean replays).

## Progress

- [x] 1. **Sanctuary** (ch11, Cultist 14): circle at caster tile, radius 3,
  600t hold, 40t pulses of (12+L)/2, 20 MP, 600t CD, kind-17 client ring
  (T-ART-09 `addCircle` caller wired) + SANCTUARY floater + cast pose.
  Zero RNG; worldHash untouched — `wave2.bwj` re-replays mm=0, no epoch bump
  (stays 30). 6 unit pins (`tests/test_sanctuary.cpp`), wave-2 spare-slot pin
  updated, choir-bot casts ch11 (both support blocks + SUMMARY c11).
  Numbers beyond the GDD 600t CD are flagged derivation (card + code comments).
  Fresh leg `logs/t161b_sanct.bwj` (L14 crypt party, 300 s, disclosed
  staging): c11=2 + replay mm=0 (60 hashes).
- [ ] 2.–6. as scoped below.

- [x] 2. **Curse of Weakness** (ch12, Cultist 12): −15% target dmg/def for
  8 s (160t), 12 MP, 120t CD, Mend range 6, enemies only (mobs + non-party
  players; self/party refused), no karma stain (flagged — stain stays on the
  killer per T-056). Purify lifts it (one law both ways). Kind-18 floater +
  cast pose. Channel table widened 12→13 (content-only, T-161 precedent).
  weakUntil/lastWeakTick stay OUT of worldHash (T-133 crownUntil precedent)
  — `wave2.bwj` re-replays mm=0, no epoch bump (stays 30). 5 unit pins
  (`tests/test_weaken.cpp`).
- [x] 3. **Raise Skeleton** (ch13, Cultist 16): kMob + ownerId thrall (row
  1026, appended — wireKind indices untouched), hp 30+10L, strikes 2+L,
  guard L/4, heel 2 / warp 18 / gate-follow, one-pet law, bond breaks at
  death/logout/recast, kills credit the master, death pays nothing
  (anti-funnel). Mobs never acquire it (retaliation tanking, flagged).
  kind-19 floater + cast pose; fallback render (art backlog). Channel table
  13→14. Bond fields OUT of worldHash — `wave2.bwj` + `t161b_sanct.bwj`
  re-replay mm=0, no epoch bump (stays 30). 7 unit pins
  (`tests/test_raise.cpp`); choir-bot casts ch13 (both blocks + SUMMARY
  c13).   Fresh leg `logs/t161b_raise.bwj` (L16 crypt party, 300 s): c13=2,
  replay mm=0 (60 hashes).
- [x] 4. **Corpse Explosion** (ch14, Gravecaller 14): spends the freshest mob
  corpse within 4 for fixed 20+3L AoE (radius 2, slam-mirror, no roll).
  Mob corpses only (players sacrosanct, thralls crumble); kin + own thrall
  immune; player kills run full PK law. Death tape (cap-32 ring, 300t meat,
  unhashed). Kind-5 hit lane — zero client change. Bots offer the shape
  (meatless casts free). `wave2/sanct/raise.bwj` re-replay mm=0, no epoch
  bump (stays 30). 4 unit pins (`tests/test_corpse.cpp`). Fresh leg
  `logs/t161b_blast.bwj` (L14 crypt party, 300 s): c14=3, replay mm=0.
- [x] 5. **Gravecaller control set** (ch15–18; unlocks 4/8/12/10): Frost
  Spike (5+L+INT + 60t slow), Wither (12 s, stacks 3, (2+L/4)/stack/20t,
  caster-credited, Purify does NOT lift rot — flagged), Terror (40t rout,
  bosses immune, no karma), Mana Shield (60 s, 1 MP per 2 dmg, drinks
  swings/bolts/slams/blasts/pulses). Slow = 4-steps-in-5 (id-staggered);
  fear owns the feet (mobs skip think, paths cleared). Fully-drunk hits
  read kind 0. Kinds 21/22/23 + cast poses. Table 15→19. All state
  unhashed — prior legs re-replay mm=0, no epoch bump (stays 30). 8 unit
  pins (`tests/test_control.cpp`); bots rotate Frost (kiter) + offer
  Wither/Terror/Shield (crypt Grave). Fresh legs `logs/t161b_control.bwj`
  (crypt: c16=6, c17=1, c18=3) + `logs/t161c_control_raider.bwj` (c15=12),
  both mm=0.
- [x] 6. **Ravager set** (ch19–23; unlocks 6/8/12/14/10): Sunder (-15% DEF
  8 s, stacks w/ Weakness, arm's reach), Bull Rush (dash ≤3 along path +
  40t prone), War Stomp (75% weapon PBAoE r2 + 20t stagger, kin + own
  thrall immune), Execute (AIMED ch22: <20% only, 8 MP, 100t CD, shared
  swing lane doubled — the passive reading would have cost an epoch;
  flagged), Second Wind (hpMax/4 over 5×20t, any post-mark hit breaks it).
  Prone = no move/no strike (all three paths gated); powerSwing extracted
  verbatim (kill texts branch on execute; ch1 gains prone/ward reads, both
  neutral). Kinds 24/25 + cast poses. Table 19→24. All state unhashed —
  all six prior legs re-replay mm=0, no epoch bump (stays 30). 6 unit pins
  (`tests/test_ravager.cpp`, incl. twin-world doubling proof). Bots offer
  the full set (fighter flow). Fresh leg `logs/t161r_ravager_crypt.bwj`:
  c19=15, c20=9, c21=6, c22=3, c23=3, replay mm=0.
- [ ] M3 party-vs-solo re-measure with the fuller kit (feeds T-157):
  `tools/t161b_m3_leg.sh` (L13 raider party, 900 s).
  - M3a (`logs/t161b_m3_m3e30.bwj`): 5/5 map-5 entries (bossSeen 10–39),
    bossKills 0, curse 46, deaths 88, party held, mm=0. Reach re-proven —
    BUT new rites unfired (crypt-only bot shape; raider path lacked the
    mirror). Bots parity mirrored (shared periods); M3b RUNNING (`m3e30b`).
  - M3b (`logs/t161b_m3_m3e30b.bwj`): 5/5 entries (bossSeen 247 total),
    bossKills 0, curse 18, slam 172, deaths 84, mm=0. Deeper engagement —
    BUT c14/c16–23 still 0: the mirror nested inside the kit-3 gate (dead
    code for kits 1/2). Mirror moved OUT with own chebM; M3c RUNNING.
  - M3c (`logs/t161b_m3_m3e30c.bwj`, bots log `t161b_m3_m3e30c_bots.log`):
    **bossKills=3**, bossSeen 20, curse 6, slam 136, deaths 12 (2–4/bot),
    maxLevel 16 (13→16 through the run), party=5 held, mm=0 (180 hashes).
    Full kit live: c11=6, c13=2, c14=9, c15=41, c16=24, c17=5, c18=1,
    c19=26, c20=14, c21=8, c22=14, c23=2. First boss kills in the gate
    series (r8–r22 + M3a/b all 0) at 1/7th the death toll. **M3 re-measured:
    PASS with the fuller kit.** Card CLOSED.

## Scope (in order, each its own commit)

1. **Sanctuary** (ch11): ground healing circle, 600t CD, radius + tick heal, decal-visible (T-ART-09 exists), no stacking.
2. **Curse of Weakness** (ch12): −15% target dmg/def + Purify interaction (one law, both ways).
3. **Raise Skeleton** (pet entity: new kind, leash, despawn law — largest).
4. **Corpse Explosion** (needs corpse entity/decay law).
5. Gravecaller control set (Frost Spike, Wither, Terror, Mana Shield).
6. Ravager set (Sunder, Bull Rush, War Stomp, Executioner, Second Wind).

Rules carried over: server-authoritative range/CD/MP/legality, journaled via existing `kSkill` channels (no new kinds unless a mechanic needs it), RNG via `sim/rng.h`, unit pins + parity notes per skill, client hotkey scheme extended (7/8 reserved), help panel updated, GDD §3 rows flipped as each ships. Epoch bump only if sim semantics move (coordinate — one bump per wave).

## Hotkeys (shipped with item 6 — humans cast the whole spine)
Shift+1..8 = ch11..18, Ctrl+1..5 = ch19..23 (one layout, all kits;
wrong-kit pages quiet no-op server-side). Help panel documents both pages.

## Acceptance (per batch)

Unit pins per skill, fresh leg firing every new skill at least once → mm=0, M3 party-vs-solo re-measured with the fuller kit (feeds T-157).
