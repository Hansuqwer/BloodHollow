# T-062 night economy + legibility floor — DONE S17 (2026-09-05)

## Context

The private-server lesson: Soma players modded permanent DAY because the
night was pitch black. Our dark must be a promise, not an eyepatch —
dangerous (T-061) AND lucrative, but never illegible.

## Scope

- Night economy at the kill: XP ×110/100 (floored), junk loot chance and
  gear-drop chance ×125/100 relative.
- Legibility floor: night overlay alpha already caps at **150/255** (59%
  at deepest blue) in `engine/render/daynight.cpp` kKeys; no key may exceed
  150. Rationale recorded here; the cap is the contractual answer to "day
  mod" pressure — night is readable, always.
- No new wire — hour is already tick-derived on both peers (T-008).

## Tests

- Night XP: paired fresh worlds, identical killer+rat, gain ratio exactly
  `*110/100` floor — pinned.
- Night loot: 400 ghoul kills per clock on one seed stratum each
  (deterministic RNG), junk count night > day AND ≤ 2× day (witness of the
  25% nudge vs a threshold floor, not a faucet).
- Tint-floor is a render-constant pin documented (tests link no raylib).

## Evidence

- tests/test_night.cpp 2 economy cases green; suite **87/87**.
- S17 smoke leg replay `OK ticks=1292 sessionCmds=209 hashes=12
  mismatches=0` — night arithmetic is strata-safe (multipliers only, no
  extra rolls), legacy epoch-4 contract kept.
