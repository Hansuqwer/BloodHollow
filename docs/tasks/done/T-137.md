# T-137 — Defender bots + live crown + M4 gate (Phase S, 4b/4)

## Context

T-136 proved register→start→breach→attune live; the live crown needs a
quiet 10 s the yard packs never grant (14 deaths blind, chase-drag
armed). This card fields defenders (the emergent contest the design
wants) and a point-blank-hold mode for crowners, then runs the M4 gate:
flip reliability + 40-bot p99.

## Scope

- Bands ARE parties (design correction mid-card): the 8-band cap counted
  INDIVIDUALS, so 12 bots left 4 unregistered permanent contesters
  freezing attunement (diagnosed via drill forensics). `siegeRegister`
  now enlists the captain's living party per call; the cap counts bands
  (M4's 40 = 8 bands). Bots form 5-man quintiles on the march (crypt
  invite/accept pattern). Pledge ids replace captain ids in Phase P
  (same shape).
- `--defenders N` (default 0): first N siege bots DEFEND.
- Hold mode (stage 5): point-blank defense inside the choreography
  (adjacent non-furniture ents → AttackRequest, no chase) + `/crown`
  loop; fighter block skipped in stage 5 (its 10-tile chase broke every
  channel in the drill). No shared-fighter surgery.
- M4 verdict legs (`tools/t137_m4_leg.sh`, rehearsal, disclosed L15
  top-up): 12 attackers + 6 defenders × ~12 min ×3 (flip count), then
  40 bots (24 atk + 16 def) × ~15 min (p99). PASS: flips 3/3 (crowned
  lines, distinct holders ideally — same-holder repeats still prove the
  loop), tick p99 < 25 ms.
- NO sim change → NO epoch bump (bot-only + leg; proven by re-replaying
  `t135.bwj` mm=0). Wire untouched.

## Acceptance criteria

- Defenders contest (attunement stalls while they hold the ring) and die
  to attacker steel; a crowner completes a live 10 s kneel; holder flips;
  M4 numbers recorded from server output.

## Tests required

- Leg evidence (verbs + outcomes + replays mm=0). Two doctest additions
  after all (shared with crown TU): re-kneel guard pin + party-band
  registration pin (both server-side, both cheap).

## Out of scope

- Pledge bands (Phase P) · vault spending · map-6 constants (PR #23
  follow-up) · Friday human wave (Phase O) · wire 237.

## Close-out (what actually shipped, beyond the plan)

- Bands-are-parties correction (cap counted individuals → permanent
  contesters) + spawn mustering (range kills marching invites) +
  band-enlist printf.
- Crown re-kneel guard (5 s spam reset the 10 s channel forever) + TU.
- Breach use-after-erase fix (name copied before despawn).
- Kiter split + hold mode + march blindness + siege terminal continue.
- M4: flips flip12/13/14 + soak2 (24v16, p99 6.6 ms). T-109 limiter
  staggering in the leg. Full forensics: devlog 0098.
