# T-135 — Weeping Castle map live (Phase S, map 6/6)

## Context

S1–S3 built the battle skeleton against staged zone-6 arenas; live zone 6
never boots (no map wired). The castle map + generator exist untracked in
this working tree from the T-123 lane (byte-stable under its generator —
verified before adopt). This card adopts them, links Thornwall ↔ castle,
and wires zone 6 into mapconv + boot. S4 rehearsals run on this card.

## Scope

- ADOPT (provenance: T-123 lane working tree; PR #23 superseded on these
  paths — flagged loudly in the PR + a comment on #23):
  `data/maps-src/weeping_castle.tmj` + `tools/mapgen/make_weeping_castle.py`
  (40×30, wall ring + south gate, courtyard, keep, Heartstone plinth,
  2 spawners, castle_gate → map 1 (21,14)).
- `tools/mapgen/make_thornwall.py`: `castle_road` portal at (21,14) →
  map 6 (20,27); thornwall.tmj regenerated (verified: ONLY the portals
  layer changed, new object id 16).
- `tools/mapconv/CMakeLists.txt`: map-6 convert + DEPENDS. Boot zone
  lists (replay mirror + live): zone 6 joins.
- Test: zone-6 load of the REAL bhmap seeds 2 gates + 1 stone (pins the
  artifact + the T-132/133 spawn code on real geometry); validator must
  report 6 maps, 0 problems (castle_gate ↔ castle_road pair).
- Epoch 24 → 25 (CORRECTION during implementation: first claimed no-bump,
  but the t128 re-replay failed 12/12 hashes — zone 6's spawners + works
  shift the entity set under old journals, the T-068 barricade precedent
  exactly). Fresh gate leg `logs/t135.bwj`; guard refuses t128 exit 4.

## Acceptance criteria

- `bh_maps` builds all six incl. castle; boot log shows zone 6 online;
  validator 6 maps / 0 problems; portal pair traverses both directions
  (validator link check); TU pins the works spawn.

## Tests required

- Extend `tests/test_siege_gates.cpp` (or crown): load
  `assets/maps/weeping_castle.bhmap` via `loadZoneFrom(6, ...)` — how is
  a bhmap loaded in tests? `loadZone` reads a FILE (repo-root-relative
  working dir per ctest props). Assert 2 gates + 1 stone standing.

## Out of scope

- Map-6 movement-blocking for gates (still staging) · siege bots (T-136) ·
  rehearsal + M4 (T-137) · PR #23's remaining content (client/map deltas —
  human reconciles) · wire 237.
