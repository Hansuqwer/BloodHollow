# T-014 — Login stub + account/character persistence (SQLite v1 schema) (S3 netcore)  [DONE 2026-09-04]

server/src/persist.* — SQLite (WAL, user_version=1) with accounts + characters tables; loginOrCreate auto-registers, name policy 3-16 [A-Za-z0-9_-]; positions savePosition()d on disconnect (verified in logs: "saved at (x,y)" per bot). ADR-0009 documents stubPasswordHash as PLUMBING ONLY (4096x salted FNV-1a, release-blocker to swap argon2id before any non-localhost build). Re-login restores last position (schema in place, pos round-trip exercised by reconnecting smoke client).

**Tests required:** per AGENTS.md. **Evidence:** verified on this branch, see
docs/devlog/0003-sprint-3-4.md.
