# T-142 — Player sheets to visible: wire class/sex + atlasFor + screenshots (Phase A, 2/3)

## Context

T-141 packed Ravager WIP sheets; the client still draws every player from
the hero placeholder. `mobSheetPaths(0)` is pinned FALSE ("players never
reach the loader", test_clientlaw) and `NetEntSnapshot` (net_client.h)
carries NO class/sex field — wiring needs a wire change, scoped here.

## Scope

- Wire: add class/sex to the entity snapshot (free byte if one exists,
  else version-byte bump + PROTOCOL_VERSION; GDD/ADR conflict → stop).
  Server packs from Entity (classId + sex source of truth — check where
  sex lives; if nowhere, character creation must capture it: scope alert).
- Client: `playerSheetPaths(class, sex)` (mobSheetPaths pattern) +
  atlasFor branch for kind 0 + `test_clientlaw` pins (ravager m/f resolve,
  unknown class falls back to hero).
- Contrast remediation call: T-141 R-LUMA FAIL (day Δ 10.7 / night Δ 4.3).
  Options: rim-light re-key (bhpix.rim_light, B5 precedent), zone-plate
  pairing, or director accept. Do not silently restyle.
- Screenshots/GIF in PR (headless + graphical CI legs).
- Generation probe: Turn 1 (2026-09-15, seeds 2001–2010, flux.1-schnell)
  REJECTED 10/10 — 0.00 green bg, painterly render, ground discs, wrong
  hands, elf ears. Lane rule: single probe gen with hardened chroma
  wording ("solid flat pure-green chroma-key RGB 0,255,0 backdrop, no
  ground, no shadow, 16-bit pixel sprite") BEFORE any batch. 10/turn cap
  stands; Cultist set untouched until the probe passes.

## Acceptance criteria

- Own + remote players render from shipped sheets on a live server;
  fallback to hero when sheet/class missing; suite + validate green;
  screenshots pasted. No epoch (render-only) unless the wire bump drags
  sim (it must not — snapshot-only).

## Tests required

- clientlaw pins (paths + fallback); loader-cache test if headless-able.

## Deviations

- TBD (wire-field choice recorded here when known).
