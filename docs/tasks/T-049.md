# T-049 record-path determinism under load (replay flake)

Context: cadence-25 hashing (BH_HASH_CADENCE=25) exposed intermittent
live-vs-replay divergence with 6-bot soaks: transient walker-tile slip at
tick ~125, sometimes a permanent split later. Control without anvil shows
the same tick-125 slip ⇒ pre-existing, NOT T-047. Replaying one journal
twice is bit-stable ⇒ the recorder drops a lane, not the reader.
Evidence: logs/2026-09-05-s10-journal-cad25.bwj (200/209 mismatch),
logs/2026-09-05-s10-ctrl.bwj pattern variants incl. a clean 30/30 run with
an identical script, live-dump/replay-dump at tick 300 show one mob alive-
at-4hp (live) vs dead (replay).
Hypothesis queue: (1) journal c-lines stamp PROCESS tick but commands effect
  world state relative to pump boundaries; a receive-time stamp + per-tick
  drain markers would make replays authoritative for timing; (2) fair-share
  one-cmd-per-session-per-tick live vs mass-apply in replay is identical only
  when no session backlogs 2+ cmds in one tick — likely violated at crackling
  end-of-fight moments; (3) walker step cancellation mid-sub-tile when a new
  path arrives.
Acceptance: cadence-25, 6-bot 300s soak × 3 consecutive runs replay 0
mismatch; cadence-100 floors stay green; unit test for "burst of N commands
in one tick produces identical permanent state in live sim & replay".
Debug lanes available: BH_DUMP_ENTS=1 (live), first-mismatch entity dump
(replay), [anvil-refuse] traces.
