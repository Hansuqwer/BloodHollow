# T-144 — M5 pre-soak signal: 60 bots × 10 min stability (Phase O, 2/2)

## Context

M5 demands 200 bots × 12 h (no leaks/crashes) + 60 fps on a 2018 MBA. A
full M5 run is director-scheduled (12 h wall, hardware). This card banks
a bounded pre-soak signal on current master-stack code (epoch 26).

## Scope

- 60 fighter bots × 600 s, live (non-rehearsal) server, scratch DB,
  `--p99-budget-ms 25`. No code, no journal of record (scratch only).

## Results (2026-09-15, `logs/soak_presoak_*.log`, local-only)

- Server exited cleanly on the soak timer; 0 ERROR/FATAL/assert lines in
  server + bot logs.
- Tick p99 max 5.1 ms over 23 samples (budget 25 ms); p50 ~1.4–1.7 ms.
- RSS flat: VmHWM = VmRSS = 8236 kB at 4 min and at end (no growth).
- 60/60 online throughout; entities 172–175 stable; 12k/10M pkts accounting
  sane; balance table printed (TTK/xp sane ranges).

## Acceptance criteria

- Signal recorded; full M5 explicitly NOT claimed (needs 200×12h +
  client fps on real hardware — director-owned, Friday-Night wave).

## Tests required

- None (no code). Evidence is the server log tail + this card.

## Deviations

- None.
