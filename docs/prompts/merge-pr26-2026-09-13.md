# Merge wave — PR #26 (T-124 merge wave + T-125 M3 gate follow-on r18→r22) (issued 2026-09-13)

*Fifth single-use authorization in the series (`merge-wave-2026-09-12.md`,
`merge-filing-2026-09-12.md`, `merge-pr18-2026-09-12.md`,
`merge-pr19-2026-09-12.md`). Same rule: this session only, explicitly **not
precedent**.*

## Authorization & scope

Director's instruction, this session: **"Make a prompt to merge. Execute
prompt."** This **reverses** the earlier standing instruction for this PR
("leave PR #26 open; I merge when I choose") and supersedes the
"never self-merge" line in the merged handover — for PR #26 only, this once.

Scoped to:

1. **Merging PR #26** (`arena/01a09c1b-bloodhollow` → master, head
   `7d47729`, base `2a813ec`) — this session's own completed work: the T-124
   merge wave for PR #25 plus the T-125 M3 gate follow-on, six 900 s legs
   (r18→r22b), the Firebolt kit-channel law, and the r21 map-5 route.
2. **The wave evidence for this merge** (card **T-126** + devlog **0087** +
   board row + this prompt), filed on `arena/01a09c1b-bloodhollow` and
   pushed. It is **not** authorized to merge itself.

Squash, per repo convention, **PR title as the subject**.

## Preconditions (verify before executing; abort on any failure)

- Master unmoved at `2a813ec` since the branch was based. ✓ required.
- PR #26 `MERGEABLE`, head `7d47729`, both `build-test` jobs green
  (ubuntu + macos — the macos/ubuntu matrix is client-ON and covers the
  three client-law TUs this sandbox cannot build). ✓ required.
- Diff scope = `tools/bots/main.cpp`, `tests/test_kits.cpp`, docs (card,
  devlogs 0084/0085/0086, handover, board, prompts), journals. **No
  server/sim change; journal epoch stays 21, schema v11, wire 237.** ✓
- Local battery green on the exact head being merged:
  `bh_tests` **209/209 · 329,058 · 0 failed**, `ctest` 2/2, duel pin
  `b273be661b54673a`, and `--replay-world` mm=0 on `m3_gate.bwj` and
  `m3_gate_r{18,19,20,21,22,22b}.bwj`. ✓
- r17 journal of record `logs/m3_gate.bwj` byte-identical to its pre-series
  md5 `b4c0cf3f3255e38071c172cc3a3336bd`. ✓

## Procedure

1. File this prompt on `arena/01a09c1b-bloodhollow`, push (pre-wave record).
2. Re-verify the preconditions on the live head.
3. `gh pr merge 26 --squash` — **PR title as the subject**, and **no
   `--delete-branch`**: `arena/01a09c1b-bloodhollow` is this session's pinned
   branch and stays alive for the wave evidence. This is a deliberate
   departure from the four earlier waves, which deleted their source
   branches.
4. Post-merge verification on the new master: new sha, and
   `git diff origin/master <branch>` **empty** — a squash of a branch whose
   parent is master must leave the trees identical, and that is the check
   that catches a bad renumber.
5. **Full battery against master's content**: suite (expect 209/209 ·
   329,058), ctest, duel pin, replay mm=0 on the r17 journal plus the six
   leg journals. Master CI green.
6. File the wave evidence: card `done/T-126.md`, devlog **0087**, board row,
   pipeline-prompt amendment. Push to the session branch. Report queue state.

## Known queue state (report, do not fix here)

PRs **#22** (T-122 pledge-lite, head `c0de5634`) and **#23** (T-123 Weeping
Castle, head `e0718d7d`, stacked on #22) both edit `tools/bots/main.cpp`
(+103/−2) and both conflict against master — verified with
`git merge-tree --write-tree` — and they carry `docs/devlog/0084-*` /
`0085-*`, which master has now taken. They stay open; their owner rebases.

## Rollback

Revert the squash commit on master. The branch is bot-only plus docs, so
nothing to migrate back: epoch stays 21, schema v11 untouched, and the r17
journal is preserved byte-identical, so no artifact is invalidated by a
revert. Source branch head recorded at `7d47729`.

## Record

Card `done/T-126.md` + devlog 0087 + board row + this prompt. Post-wave
counters: next card **T-127**, next devlog **0088**, epoch **21**, schema
**v11**, wire **237**, suite **209/209 · 329,058**.

## What this merge does **not** claim

The **boss verdict is still NOT established.** Six legs reached the font
map, `bossSeen` climbed 12 → 26, and there is not one Gravemother kill in
the record. What merged is that the reach stopped being luck (r19/r20/r21,
4/5 at 232–238 s) and that the crossing got a staged route. r22's kiter band
was **inconclusive, not measured red** — neither of its two legs reached the
code — and it was reverted for that reason. No TTK is claimed in either
direction.
