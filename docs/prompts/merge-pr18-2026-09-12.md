# Merge wave — PR #18 (M3 party-crypt gate) + pipeline filing (issued 2026-09-12)

*Third single-use authorization in the series (`merge-wave-2026-09-12.md`,
`merge-filing-2026-09-12.md`). Same rule: this session only, explicitly
**not precedent** for any other session's self-merging.*

## Authorization & scope

Director's instruction, this session: **"make a prompt to merge and Execute
it. Then make a detailed Continue prompt for executing more pr's."** Scoped
to:

1. **Merging PR #18** (`task/T-117-party-crypt-gate` → master, head
   `7d7e1b1`) — another session's completed, CI-green execution of the
   continuation prompt's Track A1 (M3 party-crypt gate leg). It is
   CONFLICTING against master for a known, benign reason: it was branched
   from `c8d8087` carrying the original snippet's "next card T-117"
   numbering, and PR #17 (the continuation-prompt filing) consumed T-117 on
   master in the meantime. Two different T-117s — a cross-session counter
   collision, the first of its kind.
2. **Filing the Continue prompt** (`docs/prompts/continue-pr-pipeline-2026-09-12.md`
   — the repeatable card→PR→merge pipeline, written for exactly this class
   of multi-session PR execution) together with this wave's evidence card
   (T-119), devlog 0081, and board row, as one squash-merged PR.

**No server/sim code changes** (PR #18 is bots-only + docs + gate leg; no
epoch bump — stays 20; no wire bump — stays 237).

## Preconditions (verified before executing; abort if any had failed)

- PR #18 CI green at head `7d7e1b1` (ubuntu+macos, full client build). ✓
- PR body carries full evidence: suite 206/206 · 329,022, duel pin
  `b273be661b54673a`, t115 replay mm=0, fresh leg replay mm=0, verdict
  (M3 exit MET — Gravemother killable, 2 kills in the 300 s leg). ✓
- Diff is bots-only: `tools/bots/main.cpp` (crypt profile), leg script,
  card, devlog, board row, `logs/t117.bwj`. No `server/`, `shared/`,
  `engine/`, or map changes. ✓
- Source session dormant: branch last pushed 17:57:54Z, PR opened 17:58,
  no activity since; work is complete per its own acceptance criteria. ✓
- PR queue has no other open PRs; nothing else bases off the branch. ✓

## The renumber (T-117 → T-118)

Master's T-117 is the continuation-prompt filing (PR #17). PR #18's card
therefore renumbers to **T-118** during the sync, per the board-wins rule:

- `docs/tasks/done/T-117.md` (theirs) → `docs/tasks/done/T-118.md`; title
  and internal references renumbered. Master's `T-117.md` (filing card) is
  kept.
- `docs/devlog/0079-t117-crypt-gate.md` → `docs/devlog/0080-t118-crypt-gate.md`
  (0079 is consumed on master by the filing devlog).
- `logs/t117.bwj` → `logs/t118.bwj` (byte-identical rename; journal content
  — including the `t117_00..04` bot names recorded inside — is untouched
  and replays bit-exact under the new filename; the replay is
  filename-agnostic).
- `tools/t117_party_crypt_leg.sh` → `tools/t118_party_crypt_leg.sh`, with
  internal references (`t117`→`t118`) and one deviation: the hardcoded
  `ROOT=/home/user/BloodHollow/build/wt/117` + `build/manual/` paths are
  made repo-relative per the harness convention (`cd "$(dirname "$0")/.."`,
  `./build/server`, `./build/tools/bots`), so the committed script runs from
  any checkout. Documented in the PR comment.
- Board README junction: master's T-117 row (filing) kept, PR #18's row
  inserted after it renumbered T-118, in card order.
- PR #18 retitled T-117→T-118, with a comment recording the renumber map.

## Procedure

1. Sync PR #18's branch: merge `origin/master` into
   `task/T-117-party-crypt-gate` (local worktree), resolve per above, push
   (additive merge commit — no force).
2. Local verification of the synced branch: offline build, suite 206/206,
   duel pin, replay of the renamed `logs/t118.bwj` → `mismatches=0`.
3. Confirm PR #18 MERGEABLE and CI green on the sync head.
4. **Squash-merge** PR #18 (normal card — squash per house style), then
   manual remote branch delete (no children exist).
5. File the Continue prompt + evidence card T-119 + devlog 0081 + board row
   on `task/T-119-pr18-merge-wave` (this branch, which carries this prompt),
   synced with post-#18 master; PR; CI green; **squash-merge** (authorized
   here); manual remote branch delete.
6. Verify master CI green and the PR queue empty.

## Rollback

- PR #18: revert its squash commit on master. The source branch was
  recorded at head `7d7e1b1` before the sync and is recreatable from that
  SHA if the remote branch was already deleted.
- Evidence PR: revert the squash commit.
- No epoch, wire, or content state is touched by this wave.

## Record

Card `done/T-119.md` + devlog 0081 + board row + the Continue prompt, all
in the evidence PR. Post-wave counters: next card **T-120**, next devlog
**0082**.
