# Merge wave — PR #19 (weapon-skill persistence) as T-120 (issued 2026-09-12)

*Fourth single-use authorization in the series (`merge-wave-2026-09-12.md`,
`merge-filing-2026-09-12.md`, `merge-pr18-2026-09-12.md`). Same rule: this
session only, explicitly **not precedent**.*

## Authorization & scope

Director's instruction, this session: **"Execute."** — given in direct
response to this session's offer to "run the next merge wave on PR #19
(weapon-skill persistence — it needs the same T-118→T-120 renumber
treatment, plus a full battery since it bumps the epoch to 21 and touches
the schema)." Scoped to:

1. **Merging PR #19** (`task/T-118-weapon-skill-persist` → master, head
   `d99fdfe`, merge-base `c8d8087`) — another session's completed,
   CI-green execution of Track B1: sword-skill persistence (schema v11,
   journal epoch 20→21, l-line v3, 3 new tests).
2. **The wave evidence PR** (card T-121 + devlog 0083 + board row + this
   prompt + a one-line continuation-prompt truth-up for the journal
   grammar), squash-merged under the same authorization.

This is the **first wave in which the target PR changes server sim
semantics** — epoch 20→21 and schema v10→v11 — so the full local battery on
the synced branch is mandatory, not optional.

## Preconditions (verified before executing; abort if any had failed)

- PR #19 CI green at head `d99fdfe` (ubuntu+macos). ✓
- Body carries full evidence: suite 209/209 · 329,052 (3 new tests), duel
  pin unchanged `b273be661b54673a`, fresh epoch-21 leg replay mm=0, old
  t115 refusal shown, DB persistence proof across relog. ✓
- Diff scope-checked: 261 lines in `server/src/persist.{cpp,h}` (additive
  v11 migration), `server/src/main.cpp` (epoch + l-line v3 + saveProgress
  plumbing), `server/CMakeLists.txt`, `tests/` (+176-line test file),
  script, docs, leg. Matches the declared card scope; no unrelated
  changes; < 400 lines. ✓
- Source session dormant: last commit 18:36:34Z, PR updated 18:37:29Z,
  evidence complete, closing note "leave OPEN, director merges". ✓
- Queue has no other open PRs; nothing bases off the branch. ✓

## The renumber (T-118 → T-120) and the binary-leg collision

Master consumed T-118 (M3 gate, PR #18) and T-119 (wave evidence, PR #20).
PR #19's card renumbers to **T-120**:

- `docs/tasks/done/T-118.md` (theirs, add/add) → `done/T-120.md`;
  master's M3-gate `T-118.md` is kept.
- `docs/devlog/0080-t118-weapon-skill-persist.md` → `0082-t120-weapon-skill-persist.md`
  (0080 and 0081 are taken on master).
- **`logs/t118.bwj` (add/add binary collision)**: master's file is the M3
  crypt leg (epoch 20); theirs is the fresh weapon-skill leg (epoch 21).
  Resolution: master's stays `logs/t118.bwj`; theirs is written to
  `logs/t120.bwj` byte-identical and becomes the leg of record (replay
  re-verified under the new name; the `t118__*` bot names recorded inside
  are history).
- `tools/t118_weapon_skill_leg.sh` → `tools/t120_weapon_skill_leg.sh`
  (internal `t118` refs renumbered; paths made repo-relative if hardcoded).
- `server/src/main.cpp` epoch comment "Fresh gate leg: logs/t118.bwj" →
  `logs/t120.bwj` (comment only).
- Board README junction: master's T-118 + T-119 rows kept; their row
  inserted renumbered T-120, card order.

## Procedure

1. File this prompt on `task/T-121-pr19-merge-wave` (pre-wave record), push.
2. Sync PR #19's branch: merge `origin/master` into
   `task/T-118-weapon-skill-persist` (additive, no force), resolve per
   above, push.
3. **Full battery on the synced branch**: offline build; suite (expect
   209/209 · 329,052); ctest 2/2; duel pin `b273be661b54673a` (combat
   math unchanged); replay renamed `logs/t120.bwj` → mm=0; epoch guard —
   `t115.bwj` and `t118.bwj` must now REFUSE (journal 20 vs build 21).
4. Confirm PR #19 MERGEABLE and CI green on the sync head; retitle to
   T-120; comment the renumber map.
5. **Squash-merge** PR #19; manual remote branch delete.
6. Evidence PR on `task/T-121-pr19-merge-wave`: card T-121, devlog 0083,
   board row, continuation-prompt journal-grammar truth-up (l-line v3).
   CI green → squash-merge; manual branch delete.
7. Verify master CI green; report queue state (new PRs from parallel
   sessions are out of scope and stay open).

## Rollback

- PR #19: revert its squash commit on master (epoch returns to 20, schema
  v11 columns are additive-inert and harmless to leave). Source branch
  recorded at head `d99fdfe` for recreation.
- Evidence PR: revert the squash commit.

## Record

Card `done/T-121.md` + devlog 0083 + board row + this prompt. Post-wave
counters: next card **T-122**, next devlog **0084**, journal epoch **21**,
suite **209/209 · 329,052**.
