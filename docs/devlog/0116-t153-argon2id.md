# 0116 — T-153 argon2id shipped (schema v16)

2026-09-16. Vendored argon2-20190702 (pinned hash, bh_argon2), m=19MiB/t=2/p=1:
hash 47 ms / verify 44 ms measured. Schema v16 PHC column; stub rows rehash
silently on login (proven incl. second-login-uses-argon); garbage refuses
reason-1 without crashing; new accounts hash at birth. Single-threaded tick
impact stated + bounded (limiter spreads bursts). Runbook §7 wave-1
--no-register default. 5 new tests, suite 330/330. Card `done/T-153.md`,
ADR-0012. Deferred: off-thread pool, true old-file migration test, 30-bot
latency number.
