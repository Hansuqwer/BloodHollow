# 0107 — T-142 wiring: class/sex to visible (wire 238, no epoch)

Spawn/delta carry `classId` (kit 1..3, 0 none) + `sex` (0 unknown until T-142b, 1 m, 2 f). Protogen base 200→201 → wire 237→238 (field addition is incompatible; count-derived version would not move). Server packs `classId` from `Entity.classId`, `sex=0` (no source of truth — CharacterRow/creation has no sex column; T-142b captures it, schema + UI + persist + journal `k`-sidecar). Client `NetEntSnapshot` +2, `net_client` delta bridges `/kit` re-sheet live, `playerSheetPaths` + `atlasForPlayer` cache, `drawRemoteEnt` branches `kind==0`. Ravager m/f resolve live, Gravecaller/Cultist resolve pending sheets (loader → hero). Sex0/unknown → hero. 299/299 (clientlaw +1), ctest 2/2, validate 0/6, `t146` mm=0 epoch 27, wire 238.

Merge repair (same session): 11 files had committed conflict markers (H1/H2 mine+steward vs T-129..T-145 siege/pledge stack at 39ac6e0) — tree did not build. Union kept mine (`spawnOreNodes` map4, `tryMine`/`/mine`, `kMine` appended LAST so 0..41 stable) + steward (map6) + full siege/pledge. `kMine` mid-enum would renumber 28..41 and break every journal; appended as last enumerator. `test_siege_stub.cpp` removed from build (H1 `siegeNow` superseded by `siegeRegister/Start`). Epoch 26→27: ore+steward shift entity set (T-068 precedent) — `t138` 12/12, `t140` 14/14 mismatches. Fresh leg `logs/t146.bwj` (5×10s wander) mm=0, guard refuses `t140` exit 4.

R-LUMA FAIL (10.7/4.3 vs 25/15) not restyled; Gravecaller probe-before-batch and 10/turn cap unchanged.
