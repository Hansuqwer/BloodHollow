# 0079 — Standing orders for the build era (T-117)

The wave closed the review era; what was missing was the paper for what
comes next. This card files it: `docs/prompts/continue-building-2026-09-12.md`
is the standing order for every build session from here on — where the game
stands, the law, the exact verification battery with expected numbers, the
build queue (M3 gate → Phase-4 spine → debt), the cheat sheet, and the
traps we paid for.

The authoring rule that mattered: **no number from memory.** Everything in
the prompt was read out of master `c8d8087` at writing time — the epoch, the
wire version, the schema version, the suite count, the duel pin, the replay
line, the entity-kind table (including the small landmine that mob 1007 is
*named* "Gravecaller", colliding with the player class), the kKit ids, the
mapIds, the journal grammar. A standing order with a stale number is worse
than none: it gets cited.

Bookkeeping note: the prompt originally named T-117/0079 as the next
counters; this filing consumed them, so the filed text reads T-118/0080.
The queue it proposes, in order: the M3 party-crypt gate (never formally
run), then the Phase-4 spine starting with weapon-skill persistence (the
T-096 gap that makes the aura grind fake progression), pledge-lite, Weeping
Castle, siege law, holdings, the bot army, anti-grief, the EK board.
Blood Moon stays a director call — the roadmap and the GDD disagree about
its phase, and that's the director's coin, not ours.

The handover (`docs/prompts/session-handoff-postwave-2026-09-12.md`) carries
the session-specific half: what the wave did, which remote branches are
stale stubs vs another session's property, and the open decisions that wait
on the director. Next session: read the prompt, run the checklist, take
T-118.
