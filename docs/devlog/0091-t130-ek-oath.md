# Devlog 0091 — T-130 EK ledger + L19 town oath (H4 night war, 2–3/3)

## What

Town identity + war fame, Helbreath-shaped: `/oath thornwall|ashen|
marrowgate|synod` (one oath at 19+, journaled `kOath`, town-crier
broadcast); war kills (both sworn, different towns) mint EK with NO karma
stain, NO red-hands line, NO guard wanted — war is not murder (GDD §1/§5).
Chaotic-victim crowd-pick drops still run (victim-state based); duels
return earlier and never mint EK; guard-murder path untouched (non-player
victims, T-078 "no faction flags").

Persist: schema v12 (`town_id`, `ek`, default 0) + `CharacterRow` + SELECT
+ `saveProgress` params + live login/logout apply. Journal carries a `w
tick idx town ek` SIDECAR (kit `k`-line precedent — the v3 l-line is
untouched; missing sidecar replays as 0,0). `ekBoard(n)` + `gm ek`
directed top-5 readout (session view; the site DB page is Phase O).

## Epoch

NONE (stays 24): zero new RNG draws anywhere on the oath/EK paths.
Proven: `t128.bwj` re-replays mm=0 on the new binary.

## Evidence

- `test_ek_oath.cpp`: 8 cases — oath gate matrix, journaled oath, war
  credit + fame broadcast, same-town/neutral stain matrix, chaotic-enemy
  + duel exclusion, board ordering/cap/readout, v11→v12 migration,
  town/EK round-trip.
- Cascade fix (expected): T-118's v10→v11 migration pin now lands v12 —
  updated to latest (columns asserted along the way).
- Full suite 247/247 green. validate_links 0/5. Duel pin unchanged
  (`b273be661b54673a`).

## Next

H4 night war DONE (moon + EK + oath). Next: Phase S siege battle logic
(scheduler → gates/Heartstone/crown → taxes → bots + M4 gate).
