# 0104 — T-143 ops boxes (no code)

Closed the agent-closable MVP §7 boxes: systemd unit (verify clean),
backup script + REAL drill (t140 DB, counts match, integrity ok), GM
runbook with verified-verbs-only discipline (two gaps carded: /ban,
free-text broadcast), crash posture = journald + coredumpctl.
Handshake refusal confirmed pre-existing (reason=4). Director keeps:
launcher, site/social, legal-lite, clean-machine boot, surveys.

Next: M5 soak signal (bounded pre-soak, full 12h is director-scheduled)
+ Friday-Night readiness (needs humans) + T-142 wiring implementation.
