# Devlog 0098 — T-137 defenders + live crown + M4 gate PASS (Phase S done)

## What

Defender bots (`--defenders N`, stages 10/11: march blind, hold the ring,
adjacent-only defense, never register/breach/crown) + point-blank hold
mode (stages 3-5/11, mobs only, no chase, no player steel) + kiter split
(even botIdx drags packs) + bands-are-parties (`siegeRegister` enlists
the captain's living party; cap counts bands; bots form quintiles,
mustering at spawn so invites land in range).

## Forensics (the expensive part, recorded so nobody re-pays it)

1. Re-crown every 5 s reset the 10 s channel forever → server guard
   (re-kneel mid-channel is a no-op) + TU pin.
2. Clustered crowners stabbed each other (lastHurtTick never cleared) →
   bots never stab players until Phase-P colors (use-after-erase in the
   breach printf found en route: name copied before despawn).
3. 12 individuals > 8-band cap → 4 permanent contesters → party bands
   (M4's 40 = 8 bands) + spawn mustering (range kills marching invites).
4. March fighter-chase dragged bots into endless wildlife → march blind;
   terminal `continue` for siege kept them out of the wander fallback
   (which fatals on unknown profiles).
5. Kennel refill (3.5 s) denies stillness → kiters + hold mode.
6. T-109 login limiter (30 new accounts/60 s) throttles mass login →
   staggered waves + count assertion in the leg.

## M4 verdict: PASS

- Flips 3/3: flip12/13/14 (12v0, 750 s) — battle, both gates, attuned,
  crowned (t137a__01 ×3, deterministic), replays mm=0.
- p99: 6.6 ms max at 40 bots (24v16 soak2, 900 s); 1.5–7.3 ms across all
  legs — budget 25 ms. Duel pin unchanged. Epoch stays 25.
- Contest rule proven inverted: 12v6/12v3 legs freeze attunement with
  holders in the ring (correct denial); 200+ defender deaths show
  attackers clear when they can reach.
- Mixed-side coordinated assault needs bands on the wire (Phase P) —
  filed, not hidden.

## Next

Phase P (pledge-lite: bands → pledge ids), Phase A (art QA pack),
Phase O (ops + Friday/M5). Siege stack: #29–#40 + this card's PR.
