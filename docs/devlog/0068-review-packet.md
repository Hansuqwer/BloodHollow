# 0068 — The last gate needs hands (T-099)

T-099 assembles; a human performs. The trade-pass slice has been "human-only" since Sprint 7, and every sprint since has re-proven the programmable half around it. This card stops re-proving and starts inviting: here is the plaza, here is the pair, here is what to touch, here is what to capture.

## Judgment calls

- Checklist over script: the packet tells the reviewer what to feel for (banner glanceability, cancel surprise, toll readability) without prescribing verdicts. A review that can only pass is theater; the shot list exists so surprises come back as artifacts, not adjectives.
- Feedback becomes cards, not commits: the packet explicitly scopes post-review changes as new work. Nothing about the trade window changes on a feeling uttered in a session — it changes on a card with acceptance criteria, like everything else in this repo.
- Ten for ten: this closes the round-2 todolist. Eleven commits, two epochs (13, 14), suite 167→173, one resurrected bug class (anvil spam), three closed watches, two priced-not-built decisions, and one proof that the Widow's lane works end to end.
