# 0103 — T-142 filed: wiring scope + Turn-1 generation triage

Turn 1 (Gravecaller Round-1, 10 gens, flux.1-schnell, seeds 2001–2010):
REJECTED 10/10 per gates — 0.00 green, painterly 3D, ground discs, wrong
hands, elf ears (f_S), glow. Quarantined OUT of plates/ (MANIFEST filed);
Runs lines written (rejected). Lesson: hardened prompt wording alone does
not move this endpoint — next: single probe gen before any batch.

Wiring finding: players can't reach the loader today by pinned law
(mobSheetPaths(0)==false) and the snapshot carries no class/sex → T-142
needs a wire change + server pack + client map + screenshots. Card filed
with the probe plan; implementation is the next work unit.
