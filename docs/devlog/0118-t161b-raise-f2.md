# 0118 — T-161b.3 Raise Skeleton (Cultist ch13) + T-157-F2 reg gate

## What
The thrall: kMob + ownerId (row 1026, appended so wireKind indices hold),
hp 30+10L, strikes 2+L, guard L/4, heel 2 / warp 18 / gate-follow, one-pet
law, bond breaks at death/logout/recast, kills credit the master
("X's thrall has slain a Y."), death pays nothing. Mobs never acquire it —
retaliation tanking (flagged limitation). kind-19 floater + cast pose;
fallback render (art backlog). Channel table 13→14 (Cultist ch13=16).

## Why this shape
- kMob reuse means zero wire (spawn/delta already carry mobId), zero new
  RNG draws on old paths (thrall strikes roll only where no journal went),
  zero hash (generic entity mix covers the body; bond fields stay out like
  crownUntil). Neutrality structural again: `wave2.bwj` + `t161b_sanct.bwj`
  re-replay mm=0 at epoch 30.
- Anti-funnel law: thrall death early-outs killMob before XP/loot/gold —
  farming your own bond buys nothing. No karma for/against thralls
  (flagged; PK-law follow-up if abused).

## Evidence
- 7 pins in `tests/test_raise.cpp` (bond/scale, all gates, one-pet replace,
  mark + master credit + thrall line, death pays nothing, logout crumble,
  replay-path dispatch). Suite 350/350 → (see final count at pass close).
- Fresh leg `tools/t161b_raise_leg.sh` (L16 crypt party, 300 s, disclosed
  staging): `logs/t161b_raise.bwj` c13=2, replay mm=0 (60 hashes).
- Choir-bot casts ch13 in both support blocks (180 s upkeep thrift).

## Also this session
- T-157-F2: `/siege-reg` gated on quintile quorum (bots-only, no sim);
  re-measure owed on the next siege gate leg. Card → `fix-landed`.

## Debt / next
- T-161b items 4–6 (Corpse/Grave/Ravager sets); M3 re-measure at card close.
- Pet nameplate tint (client can't see the bond — T-ART follow-up).
- Thrall art sheet (fallback render for now).
