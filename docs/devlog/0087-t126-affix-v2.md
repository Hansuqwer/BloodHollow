# Devlog 0087 — T-126 affix v2: table 3 → 10 (H3 loot depth, 1/5)

## What

Seven new drop-time affixes, all effect-hooked, one-per-item invariant kept:

| # | Affix | Slot | Effect |
|---|---|---|---|
| 4 | of the Ox | armor | +20 hpMax while equipped & awake |
| 5 | of Thorns | armor | reflect 2 per landed swing, floored at 1 (never kills; swings only) |
| 6 | of Focus | weapon | +4 ACC |
| 7 | of Embers | weapon | +2 dmg, +1 more at night |
| 8 | of the Vigil | any gear | +2 light radius while lit (name continuity with T-ART-03) |
| 9 | of Greed | weapon | +10% kill gold, pre-moral multiplier |
| 10 | of Mending | armor | +1 HP per OOC regen proc |

Wrong slot = flavor text only (v1 precedent: Whet-on-armor is mute).
`toggleEquip` re-syncs hpMax on **armor toggles only** (weapon toggles never
touched the cap; keeps GM/test hand-set caps intact — found live: the T-047
sunder pin hand-sets hpMax=4000 then equips a shank). Known edge flagged for
follow-up: Ox armor going dormant mid-fight keeps +20 until the next
recompute trigger.

## Why the epoch bump

The roll `rng_.range(1, kAffixCount)` widens 1..3 → 1..10, shifting every
downstream draw in journals containing gear drops. **Epoch 21 → 22.**

## Evidence

- Suite: full `ctest --preset linux-gcc` 2/2 (bh_tests incl. 10 new
  `test_affix_v2.cpp` cases, 2,000,098 assertions — incl. 1M-draw fixed-seed
  distribution, all buckets 90k..110k; blob round-trip 4..10; Thorns
  reflect + never-kill floor; Greed paired-world exact 110/100).
- Duel pin unchanged: `b273be661b54673a` (DETERMINISTIC).
- `validate_links`: 0 problems across 5 maps.
- Gate leg `logs/t126.bwj` (5 fighter bots × 60 s, 40 kills): replay
  `ticks=1241 sessionCmds=537 hashes=12 mismatches=0`.
- Epoch guard: `t120.bwj` (21) refused against build 22, exit 4.
- Leg harness note: older `*_leg.sh` scripts assume `./build/<target>`;
  the linux-gcc preset nests under `build/linux-gcc/` — `t126_affix_leg.sh`
  uses a `BUILD_DIR` variable (t120 left untouched). Valid bot profiles:
  wander|fighter|pilgrim|campaign|crypt|raider (no 'grinder').

## Next

T-127..T-130: boss uniques (Old Maw / Red Widow / Cantor Vex / Gravemother,
3 fixed-affix uniques each = 12). Then the 10 → 40 table follow-up.
