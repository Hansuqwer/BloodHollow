# Batch B3 — mobs 1001–1005 (first pass) · 2026-09-08 · **rev 2** (hound E + gnoll S re-generated, same day)

**Base:** `origin/master @ 7723929` (gate open, `B0-GATE-DECISION.md`) · **Lane:** art only · **Nothing committed.**
**Status of every asset in this batch: generation done · offline QA exit 0 · in-engine UNVALIDATED**
(the client draws every entity from `heroAtlas_`, `game.cpp:729/764`; visibility needs T-ART-01 + T-ART-05).

## 1. Batch summary

| id | mob | family | body px | native plates | derived dirs | sheet | offline QA |
|---|---|---|---|---|---|---|---|
| 1001 | Marsh Rat | vermin | 20 | SE (B0 plate) + **S** (new) | E NE N W SW NW | `mobs/1001_marsh_rat/sheet.png` 320×384, 16.6 KB | **pass** Δday 45.9 / Δnight 19.9 |
| 1002 | Feral Ghoul | ghoul-flesh | 40 | **S + E** | SE SW W NW N NE | `…/1002_feral_ghoul/sheet.png` 19.2 KB | **pass** Δ 55.1 / 24.0 |
| 1003 | Hollow Hound | hound | 26 (S) · **26 (E/W, rev 2: compact profile plate, no longer width-limited)** | **S + E (E re-gen)** | as above | `…/1003_hollow_hound/sheet.png` 22.3 KB | **pass** Δ 43.1 / 18.7 (gamma 0.72) |
| 1004 | Plague Bat | vermin | 18, hover 12 (body bottom row 29, shadow on 45) | **S + E** | as above | `…/1004_plague_bat/sheet.png` 10.9 KB | **pass** Δ 52.5 / 22.8 (gamma 0.62) |
| 1005 | Bonepicker Gnoll | grave-goods | 42 (rev 2: club horizontal → S body 37, E 33; width-limited by the club length, see §4) | **S (re-gen) + E** | as above; W-side = mirror **with the club pushed behind the body** (right shoulder = far side) | `…/1005_bonepicker_gnoll/sheet.png` 31.7 KB | **pass** Δ 40.4 / 17.5 |

Anims per sheet: walk 4 @10 · attack 3 @12 · die 3 @8 → 10 cols × 8 rows, `anchorY: 42` in every block
(`sheet.json` layout is byte-for-byte the `atlas.draft.json` layout from B0.5, so the drafts were retired).
All ≤32 colours per frame (family ramps from `palettes/families.json`), all common budgets ≤40 KB.
Deterministic: `sh tools/atlaspack/b3_build.sh` twice → identical md5s.

**What is honest about "first pass":** only S and E are native generations. SE/SW/W/NW/N/NE are
pixel-op derivations (`derivation.json` per mob lists each), and every walk/attack/die frame is a
bob/shear/lunge/flop of the per-direction base — enough for T-ART-01/05 bring-up and the crowd/R-LUMA
tests, **not** final animation. The prompt.md hand-fix lists still apply in full.

## 2. QA record

| asset | triptych | audit |
|---|---|---|
| 1001 sheet | `docs/research-notes/qa/b3_1001_marsh_rat_qa_3x.png` | `…/b3_1001_marsh_rat_audit.json` |
| 1002 sheet | `…/b3_1002_feral_ghoul_qa_3x.png` | `…/b3_1002_feral_ghoul_audit.json` |
| 1003 sheet | `…/b3_1003_hollow_hound_qa_3x.png` | `…/b3_1003_hollow_hound_audit.json` |
| 1004 sheet | `…/b3_1004_plague_bat_qa_3x.png` (`--hover 12`: flight anims gate at row 30, die on 42) | `…/b3_1004_plague_bat_audit.json` |
| 1005 sheet | `…/b3_1005_bonepicker_gnoll_qa_3x.png` | `…/b3_1005_bonepicker_gnoll_audit.json` |
| 3-peer lineup (day / engine night / grey) | `…/b3_lineup_3x.png` | `…/b3_silhouette_metrics.json` |

Gates applied (all against the B0 fields/mud plate, terrain luma 50.3): R-LUMA body-excl-outline Δ ≥ 25 day,
≥ 15 after the 02:00 overlay; colours ≤ 32 every frame; loader-rule replica clean; feet on anchor ±2 all frames.

Silhouette test (grey row of the lineup, `b3_silhouette_metrics.json`): rat w/h 1.55 low-horizontal ·
ghoul 0.48 tall-thin · hound 1.08 wide wedge · bat 1.29 airborne W with 13 px of air under it · gnoll 0.94
tall-and-wide with the club line. Five distinct reads at 1×; the 20-mobs.md roster claims hold.

**Failures caught and fixed by the gate (recorded, not hidden):** hound Δnight 12.8 and bat Δday 23.4 /
Δnight 10.2 on the first build → both bodies lifted via the palette gamma (0.72 / 0.62), not by touching the
outline or alpha; bat die-frames failed the feet gate because the corpse lands on the ground — the QA tool
now checks die frames at the anchor and flight frames at anchor−hover (`bh_qa_sheet.py --hover`).

## 3. LICENSES

Three rows added to `assets/LICENSES.md`: B3 native plates (model **undisclosed**, prompts in each
`prompt.md` §Runs), B3 sheets (derived, tool chain named), B3 QA artefacts. No ancestor art used.

## 4. Open decisions / flags

| tag | item |
|---|---|
| ⟨SIBLING⟩ | **T-ART-01 + T-ART-05** are the only thing between these sheets and the screen (per-kind atlas lookup: `wireKind = kMobs index + 1`, `REGISTRY.md`). T-ART-04 (attack/die playback) after. |
| ⟨SIBLING⟩ | `anchorY` is written in every anim block; the loader ignores it today (fine for 32×48, all anchors = 42). |
| ~~⟨DIRECTOR⟩~~ **resolved rev 2** | Hound E/W profile was 18 px (1.8:1 plate). Re-generated E with a "compact, short-coupled" pose → 32×26 at body 26, `native_fit.E.width_limited = false`. Old plate overwritten (not kept — it was never a source of anything shipped). |
| ~~⟨DIRECTOR⟩~~ **resolved rev 2 (partial)** | Gnoll S re-generated with the club horizontal across the shoulders: body now 37 px (was 34), the fit is now *width*-limited by the club length instead of height-limited by it. Accepting 37: the hunch + club line is the silhouette; a 42-px gnoll would need the club trimmed. `--asym-box 0,0,15,13` (E plate, unchanged) still valid. |
| ⟨DIRECTOR⟩ | Native **SE** plates (the prompt's third native dir) were skipped for budget; SE is derived from S. Worth one generation per mob before B9. |
| ⟨BLOCKED⟩ | In-engine validation: no raylib/cmake in this sandbox; the loader-rule replica is the only proof. |

## 5. Hand-off

Uncommitted tree, all in lane (`git status --porcelain -uall`: 97 entries; `assets/aigen/.gitignore` keeps the
regenerable `cells/` out). Suggested commits:

```
git add tools/atlaspack/bh_mob_sheet.py tools/atlaspack/b3_build.sh tools/atlaspack/bh_qa_sheet.py tools/atlaspack/make_style_tile.py
git commit -m "art(tools): mob sheet chain (native S/E → 8-dir × 10-frame cells), plate floor clamp, QA hover rule"
git add assets/aigen docs/art docs/research-notes assets/LICENSES.md
git commit -m "art(B3): mobs 1001-1005 first-pass sheets from native S+E plates; gate consolidation D2/D5/D7/D9; batch record"
```

Reproduce: `sh tools/atlaspack/b3_build.sh` (exit 0 = all five sheets pass), then view `docs/research-notes/qa/b3_lineup_3x.png`.
